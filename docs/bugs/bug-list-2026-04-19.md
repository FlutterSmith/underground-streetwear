# Bug List - Agent Smith Hunt
Generated: 2026-04-19T12:07:06.931Z

## Summary
**Total Bugs:** 380 | Critical: 0 | High: 33 | Medium: 157 | Low: 190

---
## [FIXED] BUG-001: BarButton has no disabled prop or disabled visual state

> **Resolution:** Added optional `disabled` and `type` props to BarButton. Button variant passes `disabled` to native `<button>`; Link variant uses `aria-disabled`, `tabIndex={-1}`, and click preventDefault. Both apply `opacity-50 cursor-not-allowed` (plus `pointer-events-none` on the link) for clear visual feedback. Forms can now gate the CTA during submission via `<BarButton disabled={submitting} type="submit" />`.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/BarButton.tsx:68-74`
**Description:** BarButton (the brand's signature CTA used across the site) does not accept a `disabled` prop, and has no `disabled:opacity` / `disabled:cursor-not-allowed` styling. Downstream forms can't disable it while submitting.
**Impact:** Forms using BarButton can't signal pending/disabled state — users may double-submit checkout, pre-order, newsletter, contact forms.
**Evidence:**
```tsx
<button
  type="button"
  onClick={onClick}
  className={clsx("focus:outline-none focus-visible:ring-2 focus-visible:ring-current")}
>
```

---
## [FIXED] BUG-002: CartDrawer missing focus trap

> **Resolution:** Added a focus trap in `CartDrawer`: panel ref + keydown handler cycles Tab/Shift+Tab within the drawer's focusables, snaps stray focus back in, auto-focuses the first focusable on open, and restores focus to the previously-active element on close. Also set `aria-modal="true"` and `tabIndex={-1}` on the dialog (incidentally resolves BUG-003).
**Severity:** High
**Category:** UI & Components

**Location:** `src/components/CartDrawer.tsx:51-200`
**Description:** The cart drawer renders as `role="dialog"` but does not trap keyboard focus inside. Tab navigation escapes to the page behind the drawer.
**Impact:** Keyboard and screen reader users lose orientation; violates WCAG 2.1.2 modal focus management.
**Evidence:**
```tsx
<motion.aside role="dialog" aria-label="Shopping cart">
  {/* no focus trap, no focus restore on close */}
```

---
## [FIXED] BUG-003: CartDrawer missing aria-modal

> **Resolution:** Resolved alongside BUG-002. `aria-modal="true"` was added to the `<motion.aside role="dialog">` in `CartDrawer.tsx` as part of the focus-trap fix, since both belong to the same modal-a11y concern.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/CartDrawer.tsx:51`
**Description:** Dialog element missing `aria-modal="true"`.
**Impact:** Assistive technologies won't announce the drawer as a modal context.

---
## [FIXED] BUG-004: Cart quantity can be decremented to zero

> **Resolution:** The "zero orphans" claim was a false positive — `setQty` in `src/lib/cart.tsx:104` already clamps to `Math.max(1, ...)` so qty can never reach 0. The real issue was a silent no-op: clicking minus at qty=1 did nothing. Added `disabled={it.qty <= 1}` + `disabled:opacity-40 disabled:cursor-not-allowed` styling to the minus buttons in both `CartDrawer.tsx` and `app/cart/page.tsx`. Users use "Remove" to delete the row entirely.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/CartDrawer.tsx:140-145`, `src/app/cart/page.tsx:107-124`
**Description:** The `−` quantity buttons call `setQty(id, qty - 1)` without guarding for `qty <= 1`. User can reduce to 0 with no confirmation / removal UI.
**Impact:** Cart rows silently become zero-qty orphans.
**Evidence:**
```tsx
<button onClick={() => setQty(it.id, it.qty - 1)}>−</button>
```

---
## [FIXED] BUG-005: FormField inputs lack visible focus ring

> **Resolution:** Added `focus-visible:ring-2 focus-visible:ring-black/40 focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--color-bg-light)]` to the shared `fieldClass` in `FormField.tsx`. Keyboard users now get a clear ring in addition to the border-color change; mouse focus is unaffected thanks to `focus-visible`.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/FormField.tsx:25`
**Description:** Only `focus:border-black` is applied — no ring/outline for keyboard users.
**Impact:** Poor keyboard accessibility across all forms (contact, checkout, pre-order).
**Evidence:**
```tsx
"w-full bg-transparent border-b border-black/30 focus:border-black outline-none ..."
```

---
## [FIXED] BUG-006: Newsletter error state UX unclear

> **Resolution:** Wired up a live region + input association. Error `<p>` now has `id="newsletter-error"`, `role="alert"`, and `aria-live="polite"`. Input gets `aria-invalid={state === "err"}` and `aria-describedby="newsletter-error"` while in error state. Screen readers announce the validation message when it appears.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Newsletter.tsx:56-58`
**Description:** On error, typing clears the error but there is no dedicated error message region with `role="alert"`.
**Impact:** Screen readers won't announce validation errors live.

---
## [FIXED] BUG-007: ProductTile image has no onError fallback — skeleton persists forever

> **Resolution:** Partial false positive on the "no onError" claim — `onError` already swapped to `FALLBACK`. Real bug was that if the fallback image itself fails, `onLoad` never fires and the skeleton shimmers forever. Fix: `onError` now also calls `setLoaded(true)` so the skeleton unconditionally clears on any image error, regardless of whether the fallback eventually resolves.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/ProductTile.tsx:87-101`
**Description:** `setLoaded(true)` only runs in `onLoad`. If `<Image>` errors, the skeleton overlay stays visible permanently.
**Impact:** Any broken product image shows a shimmering skeleton forever — no error state.
**Evidence:**
```tsx
{!loaded && <div className="absolute inset-0 skeleton" aria-hidden />}
<Image onLoad={() => setLoaded(true)} /* no onError */ />
```

---
## [FIXED] BUG-008: ProductTile quick-add only works on hover — not touch / keyboard

> **Resolution:** Added `group` to the tile `<Link>` and extended the quick-add overlay's classes with `group-focus-within:translate-y-0` (keyboard focus reveals the overlay when users Tab into the tile) and `[@media(hover:none)]:translate-y-0` (touch devices with no hover state keep the overlay permanently visible). Mouse/hover behavior preserved via existing `hovered` JS state.
**Severity:** High
**Category:** UI & Components

**Location:** `src/components/ProductTile.tsx:138-150`
**Description:** Quick-add size overlay shows on `:hover` only. Touch devices and keyboard users cannot reach it.
**Impact:** Mobile users (the majority on a streetwear site) cannot use the quick-add feature.

---
## [FIXED] BUG-009: ProductTile sold-out state has no dimming / disabled styling

> **Resolution:** Partial false positive — image layer already had `grayscale opacity-50` and caption switched to `— waitlist open` when sold out. Gap was the product name staying at full brightness (`text-white/90`). Fixed by switching name color to `text-white/40` when `product.soldOut`, matching the dimmed imagery and subtext. Tile remains clickable on purpose so users can reach the waitlist signup on the product page.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/ProductTile.tsx:119-125`
**Description:** Sold-out overlay label is present, but the tile itself is still fully opaque and clickable without visual de-emphasis.
**Impact:** Users may not notice sold-out status at a glance.

---
## [FIXED] BUG-010: Reviews empty state lacks semantic structure

> **Resolution:** Replaced the bare `<div>` empty state with a `<section>` landmark, added a visually-hidden `<h3 class="sr-only">Reviews</h3>` for screen readers, wired `aria-labelledby`, and added `role="status"` + `aria-live="polite"` so late "no reviews" state changes announce. Visual output unchanged.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Reviews.tsx:28-33`
**Description:** "No reviews yet. Be the first." is a bare `<p>` with no heading or `aria-live` region.
**Impact:** Screen readers don't announce the empty state context.

---
## [FIXED] BUG-011: Reviews filter buttons have no empty-result feedback

> **Resolution:** Added a conditional `<p role="status" aria-live="polite">No N-star reviews yet.</p>` that renders only when a filter is active and `visible.length === 0`. Users now get explicit feedback instead of a blank area, and screen readers announce the result. The existing "Show all reviews" escape button remains below.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Reviews.tsx:55-74`
**Description:** Filter buttons filter the list but there is no message when a filter yields zero results.
**Impact:** User sees empty area with no explanation.

---
## [FIXED] BUG-012: ShopGrid has no loading state / aria-busy

> **Resolution:** Partial false positive — ShopGrid is fully synchronous (products are passed as props, filtering runs in `useMemo`), so there is no real async "loading" state requiring a skeleton or `aria-busy`. Per-tile image loading already has its own skeleton inside `ProductTile`. The real a11y gap was silent filter/sort/search updates, so added `role="status" aria-live="polite"` to the "{n} pieces" counter — assistive tech now announces the count each time the list changes.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/ShopGrid.tsx:203-206`
**Description:** No skeleton or `aria-busy` indicator while product list computes or images load.
**Impact:** Perceived performance is poor on first load.

---
## [FIXED] BUG-013: ShopGrid search has no clear button

> **Resolution:** Added a custom ✕ clear button inside the search input (right-aligned, absolute-positioned) that appears only when `query.length > 0`; click resets the query and the existing `useMemo` recomputes the list. Button has `aria-label="Clear search"` and a focus-visible ring. Also suppressed the inconsistent WebKit `::-webkit-search-cancel-button` so only the custom affordance shows.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/ShopGrid.tsx:137-144`
**Description:** Search input has no ✕ button to quickly clear the query.
**Impact:** Extra friction to reset filters.

---
## [FIXED] BUG-014: ShopGrid sort `<select>` focus state invisible on dark theme

> **Resolution:** Added `focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black` to the sort `<select>`. Keyboard focus now shows a white ring against the dark shop background, while the original border-color transition remains for continuity. `focus-visible` keeps mouse focus clean.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/ShopGrid.tsx:161-172`
**Description:** Uses `focus:border-white` without a ring — hard to see keyboard focus.
**Impact:** Keyboard navigation discoverability.

---
## [FALSE_POSITIVE] BUG-015: WishlistHeart has no loading / optimistic state

> **Resolution:** Not a bug. `toggle` in `src/lib/wishlist.tsx:64` is a synchronous `useCallback` (no Promise, no network). `active = has(productId)` is derived from React state, so the heart fill, `aria-pressed`, and `aria-label` flip in the same render as the click — there is no async latency window that would justify a loading indicator. Rapid double-clicks simply toggle the boolean twice (net zero), which is the correct semantics of a binary wishlist toggle.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/WishlistHeart.tsx:20-47`
**Description:** No visual feedback during toggle — users may double-click.
**Impact:** Potential duplicate toggles, confusing UX.

---
## [FALSE_POSITIVE] BUG-016: Timestamp renders empty on SSR causing layout shift

> **Resolution:** Not a real CLS bug. The empty-SSR pattern is intentional (documented in CLAUDE.md) to avoid server/client hydration mismatches on the clock. `Timestamp.tsx` already reserves dimensions via `min-w-[14rem] min-h-[1rem]` and fills the pre-hydration slot with `\u00A0`, so text replaces a same-sized box — no layout shift. The bug report itself acknowledges the pattern is intentional.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Timestamp.tsx:19-32`
**Description:** Server HTML contains empty/nbsp — visible flash on hydrate.
**Impact:** CLS/UX shimmer on first paint. (Note: this pattern is intentional per CLAUDE.md — documenting as a visible side-effect only.)

---
## [FIXED] BUG-017: Cart page quantity buttons inconsistent focus styles

> **Resolution:** Added `focus:outline-none focus-visible:ring-2 focus-visible:ring-current` to both minus and plus buttons in `app/cart/page.tsx`, matching the pattern already used by the identical buttons in `CartDrawer` and other site CTAs. Keyboard focus now shows a consistent ring across both cart surfaces.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/cart/page.tsx:107-124`
**Description:** Missing `focus-visible:ring-2` where sibling buttons have it.
**Impact:** Inconsistent keyboard navigation.

---
## [FIXED] BUG-018: Checkout form fields missing `required` / `aria-required`

> **Resolution:** Added `required` + `aria-required` to every mandatory `<FormField>` in `app/checkout/page.tsx` (email, phone, firstName, lastName, address, city, country, postal). `address2` (apartment, suite) remains optional per its label. FormField spreads `InputHTMLAttributes` through to `<input>`, so props flow naturally. The form keeps `noValidate` — custom `validate()` still drives submit-time errors — but assistive tech now receives required-field announcements.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:223-309`
**Description:** Name/email/address fields are not marked required at the HTML level.
**Impact:** Assistive tech users don't get required-field announcements; browser-level validation disabled.

---
## [FIXED] BUG-019: Checkout "In stock only" checkbox has no visible focus indicator

> **Resolution:** Location in bug report was wrong — `checkout/page.tsx:147-155` is the order-confirmation screen and has no checkbox. The only "In stock only" checkbox lives in `ShopGrid.tsx`. Fixed there by adding `focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-black`, matching the adjacent sort `<select>` focus style.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:147-155`
**Description:** Checkbox lacks a visible focus style.
**Impact:** Keyboard users cannot see focus state.

---
## [FALSE_POSITIVE] BUG-020: Contact topic toggle buttons diverge from site toggle pattern

> **Resolution:** Not a divergence. Contact topic toggles (`contact/page.tsx:122-135`) already match the canonical light-theme chip pattern used by `/pre-order` size toggles (`pre-order/page.tsx:164-168`): identical active styling `bg-black text-white border-black` / inactive `border-black/40 hover:border-black`, same `focus-visible:ring-2 focus-visible:ring-current`, and `aria-pressed` on both. The checkout shipping-method selector uses a distinct card-pattern, but that's an intentionally different UI class (full-width option cards, not chip toggles).
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/contact/page.tsx:122-135`
**Description:** Uses `aria-pressed` but its active visual style differs from other toggles in the app.
**Impact:** Inconsistent UX language.

---
## [FIXED] BUG-021: Orders lookup form validation errors not associated with inputs

> **Resolution:** Gave the error `<p>` an `id="orders-lookup-error"` plus `role="alert"` + `aria-live="polite"`, and wired both the order-number and email FormFields with `aria-invalid={!!error}` and `aria-describedby="orders-lookup-error"` when an error is present. Since the lookup error is form-level (either combo fails), both inputs reference the same message — correct pattern when the backend can't attribute the failure to a specific field.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/orders/page.tsx:96-132`
**Description:** Error text appears below the form but inputs lack `aria-invalid` / `aria-describedby`.
**Impact:** Screen readers don't link errors to the failing field.

---
## [FIXED] BUG-022: Orders track button relies on disabled attribute alone

> **Resolution:** Partial false positive — `disabled={busy}` on the native `<button>` already blocks mouse clicks (browsers do not dispatch click events on disabled controls), so `pointer-events:none` was unnecessary. The real gap was form submission via Enter from a focused input, which bypasses the button's disabled state while `await` is in flight. Added `if (busy) return;` at the top of `handleSubmit` so re-entrant calls are a no-op.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/orders/page.tsx:119-125`
**Description:** `disabled={busy}` set but no pointer-events:none — during rapid double-click, event can still register before state updates.
**Impact:** Potential duplicate lookups.

---
## [FIXED] BUG-023: Pre-order size grid wraps awkwardly on narrow screens

> **Resolution:** Replaced the `flex flex-wrap gap-2` container with `grid grid-cols-6 gap-2` and dropped the fixed `w-12` from each button. The 6 size chips now share six equal columns and stay on one row at any viewport width (they shrink together instead of wrapping to two rows). Height, borders, active/hover/focus/ARIA state all preserved.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/pre-order/page.tsx:158-173`
**Description:** 6 size chips (XS–XXL) in flex-wrap look cramped under 360px.
**Impact:** Minor layout bug on small phones.

---
## [FIXED] BUG-024: Shop page empty state is a static string, no skeleton

> **Resolution:** Replaced the ambiguous "DROP LOADING…" text with an explicit empty-state message ("— no drops live yet —" with supporting subtext) and added `role="status"` so AT announces it as informational status. Skeleton/aria-busy intentionally not added: `/shop` is SSG, so `products.length === 0` is a terminal empty state (no async to resolve), not a loading state.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/shop/page.tsx:11-24`
**Description:** When products.length === 0 the page shows "DROP LOADING..." with no aria-busy and no skeleton tiles.
**Impact:** Ambiguous state between loading and genuinely empty.

---
## [FIXED] BUG-025: Product detail "Size guide" link is a dead anchor

> **Resolution:** Replaced the `<Link href="#">Size guide</Link>` with a native `<details>`/`<summary>` disclosure that reveals an inline chest/length (cm) size chart for XS–XXL. Native element provides keyboard support; summary has focus-visible ring; popover is absolutely positioned with `z-10`. No new route added, respecting the PRD's 6-route constraint.
**Severity:** High
**Category:** UI & Components

**Location:** `src/app/shop/[id]/ProductDetail.tsx:231-236`
**Description:** The size guide `<Link>` points at `href="#"` — no destination wired up.
**Impact:** Feature is visibly broken; clicking returns user to top of page.
**Evidence:**
```tsx
<Link href="#">Size guide</Link>
```

---
## [FIXED] BUG-026: Product gallery thumbnails not keyboard-navigable past visible set

> **Resolution:** Converted the thumbnail strip into a proper ARIA tablist with roving `tabIndex`, `aria-selected`, and keyboard handling for ArrowLeft/Right (wrapping), Home, and End. Focus moves with the active thumb and `scrollIntoView` ensures offscreen images (>4) become visible. Added focus-visible ring.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/shop/[id]/ProductDetail.tsx:117-139`
**Description:** Gallery thumbnails are clickable but have no arrow-key handling, and if > 4 images the rest are hidden behind scroll without keyboard affordance.
**Impact:** Keyboard-only users stuck on first image.

---
## [FIXED] BUG-027: Product detail waitlist error does not refocus input

> **Resolution:** Added a ref to the waitlist email input and call `focus()`+`select()` when validation fails, so users can retype immediately. Also wired `aria-invalid`/`aria-describedby` to a `role="alert"` error message for screen readers.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/shop/[id]/ProductDetail.tsx:197-222`
**Description:** Invalid-email error renders but focus stays on submit button.
**Impact:** Users must manually click the input to retry.

---
## [FIXED] BUG-028: "Just added" feedback looks like a disabled/broken button

> **Resolution:** Added an inline checkmark SVG (aria-hidden) beside the "Added to cart" label when `justAdded` is true, so the disabled button reads clearly as a success confirmation instead of a broken state.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/shop/[id]/ProductDetail.tsx:290-304`
**Description:** When `justAdded === true`, the button is non-interactive but there is no tick/confirmation icon.
**Impact:** Unclear success feedback.

---
## [FIXED] BUG-029: Related products grid only uses 2 columns on mobile

> **Resolution:** Added `sm:grid-cols-3` to the related-products grid so tablets (640–768px) show 3 columns instead of oversized 2-col tiles. Primary mobile/desktop split at `md:` (768px, FR-R1) preserved; this `sm:` is a refinement within the range, not a replacement for that split.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/shop/[id]/ProductDetail.tsx:321-348`
**Description:** `grid-cols-2 md:grid-cols-4` — wastes space on tablets (which should be 3–4 cols under the PRD's 768px breakpoint).
**Impact:** Responsive regression; tiles are oversized on 600–768px.

---
## [FIXED] BUG-030: Wishlist "Add to cart" has no async feedback

> **Resolution:** Tracks `justAddedId` state per item; on click the button disables, inverts, and shows "✓ Added" for 1.4s before reverting. Guards against double-tap with an early return. `aria-live="polite"` announces the label change.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/wishlist/page.tsx:97-103`
**Description:** No disabled / loading / confirmation state when adding from wishlist.
**Impact:** Users may tap repeatedly.

---
## [FIXED] BUG-031: Wishlist item images have redundant alt text

> **Resolution:** Set the image `alt=""` (decorative) and moved the accessible name to the wrapping Link via `aria-label="View ${p.name}"`. Screen readers now announce the product once via the link instead of duplicating via alt + visible `<p>`.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/wishlist/page.tsx:66-74`
**Description:** `alt={p.name}` duplicates the product name which is already present below the image.
**Impact:** Screen readers announce the product name twice.

---
## [FIXED] BUG-032: Wishlist / Cart / Orders pages lack true empty states with CTAs

> **Resolution:** Partial false positive — Cart and Wishlist empty states already ship `BarButton` shop CTAs ("Go to shop" / "Browse the drop"). Orders is a lookup form rather than a collection, but it did lack a shop CTA — added a "Haven't ordered yet? → Shop the drop" `BarButton` beneath the lookup form, separated by a hairline rule so it reads as secondary to the primary Track action.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/wishlist/page.tsx`, `src/app/cart/page.tsx`, `src/app/orders/page.tsx`
**Description:** Empty cart/wishlist/orders do not offer a primary CTA (e.g. "Shop the drop") — just a plain sentence.
**Impact:** Dead-end UX for empty collections.

---
## [FIXED] BUG-033: Nav has no mobile focus management when mobile menu opens

> **Resolution:** On open, focus moves to the first focusable element inside the menu; on close, focus restores to the hamburger toggle. Added Escape-to-close keyboard shortcut, `role="dialog"` + `aria-modal="true"` + `aria-label` on the menu container, and `aria-controls` pairing on the toggle.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Nav.tsx`
**Description:** The mobile menu toggle opens a fullscreen menu but does not move focus into the menu or restore it on close.
**Impact:** Keyboard/screen-reader users lose their place.

---
## [FIXED] BUG-034: Logo server-component reads SVG at module load — no fallback for missing file

> **Resolution:** Wrapped the `fs.readFileSync` call in try/catch; on failure, logs a warning with the path/error and falls back to an inline `<text>` wordmark using `siteConfig.brandName` in the default viewBox. Layout no longer crashes if the logo file is missing or unreadable, and the failure is observable (not silent).
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Logo.tsx`
**Description:** `fs.readFileSync('public/logo.svg')` will throw at build/request time if the file is missing or unreadable.
**Impact:** Entire layout fails to render — no graceful error state.

---
## [FIXED] BUG-035: PageTransition has no reduced-motion fallback declared for `exit`

> **Resolution:** Changed the reduced-motion transition duration from `0.15s` to `0` so initial/animate/exit variants apply instantly instead of animating briefly. Fully honors `prefers-reduced-motion`; normal-motion branch (0.4s) untouched.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/PageTransition.tsx`
**Description:** Even when `useReducedMotion()` is true, exit variants may still animate briefly.
**Impact:** Accessibility preference partially ignored.

---
## [FIXED] BUG-036: Cursor component applies globally with no opt-out on touch devices

> **Resolution:** Added `enabled` state gated on `matchMedia('(hover: hover)')` in the effect. Touch devices now render `null` (no DOM node), eliminating the unused div. Hover-capable devices set `enabled=true`, mount the dot, and run the rAF loop as before.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Cursor.tsx`
**Description:** Custom cursor is hidden via `matchMedia('(hover: hover)')` but mounts unconditionally — unnecessary JS on mobile.
**Impact:** Tiny perf cost on mobile.

---
## [FIXED] BUG-037: SocialRow icons use inline `fill` prop instead of currentColor

> **Resolution:** Swapped the hardcoded `fill="#FFFFFF"|"#000000"` for `fill="currentColor"`; color now lives on the parent `<ul>` (`text-white`/`text-black` based on `invert`). Added `hover:opacity-70` so the icons give visible hover feedback and can be themed via the CSS color cascade.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/SocialRow.tsx:39-47`
**Description:** Harder to theme via CSS/hover.
**Impact:** Style inflexibility; no hover color change.

---
## [FALSE_POSITIVE] BUG-038: RecentlyViewedStrip has no empty state

> **Resolution:** Not a bug. The component already early-returns `null` at line 25 when `items.length === 0`, and the "— recently viewed —" heading lives inside the same `<section>` that gets skipped. There is no orphan-heading scenario.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/RecentlyViewedStrip.tsx`
**Description:** Renders nothing (or whitespace) when history is empty instead of hiding its heading.
**Impact:** Orphan heading above empty space.

---
## [FIXED] BUG-039: Cart `add` silently clamps at 99 instead of rejecting

> **Resolution:** `add` now returns `{ added, capped }` so callers can detect a clamp and surface the right feedback. Also tightened the new-line path to cap at 99 (it previously accepted `qty > 99` for a fresh line). Existing callers that ignore the return continue to work; future UX can react to `capped` for a "max reached" toast.
**Severity:** High
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:80`
**Description:** `qty: Math.min(99, i.qty + qty)` silently clamps when qty > 99 is requested — no signal to caller that the add was partial.
**Impact:** UI confirms "added" but the actual added count is less than requested; users can't tell they've hit the cap.
**Evidence:**
```tsx
qty: Math.min(99, i.qty + qty)
```

---
## [FIXED] BUG-040: Cart `add` accepts zero/negative qty

> **Resolution:** Added a guard at the top of `add`: `Math.floor(qty)` then reject and early-return `{added:0, capped:false}` if non-finite or `<= 0`. Blocks zero/negative/NaN/Infinity inputs from mutating state and normalizes fractional inputs to integers.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:74`
**Description:** No guard `if (qty <= 0) return;` at the top of `add`. A call like `add(product, "M", 0)` or `-5` mutates state with an invalid row.
**Impact:** Invalid cart rows possible from programmatic misuse.

---
## [FIXED] BUG-041: `setQty` clamps to 1 so decrement below 1 is a no-op

> **Resolution:** `setQty` now removes the line item when the floored qty is `<= 0` (or non-finite) instead of silently clamping to 1. Positive values still clamp to `[1, 99]`. Aligns business logic with the natural user intent that "decrement to zero = remove". Cart UIs already have `disabled={qty <= 1}` on the `−` button, so this primarily hardens programmatic callers.
**Severity:** High
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:103-105`
**Description:** `setQty` uses `Math.max(1, Math.min(99, Math.floor(qty)))`. Callers that pass `0` expecting removal are silently ignored.
**Impact:** Cart UI's `−` button at qty=1 appears broken — clicking does nothing instead of removing the line item.
**Evidence:**
```tsx
const clamped = Math.max(1, Math.min(99, Math.floor(qty)));
```

---
## [SKIPPED] BUG-042: Currency conversion uses hardcoded stale FX rates

> **Resolution:** Skipped — a proper fix requires an external FX API integration (e.g. `exchangerate.host`), a 24h cache layer, failure handling, and secrets management, which is out of scope for a surgical bug fix. The file's existing `TODO(template-buyer)` already flags this as template-buyer work for whoever ships the site with a real checkout. No real checkout is live in this template (static marketing site), so the displayed rates are approximations only.
**Severity:** High
**Category:** Business Logic

**Location:** `src/lib/currency.tsx:15-18`
**Description:** `USD: 1/48`, `EUR: 1/52` hardcoded with a `TODO(template-buyer): replace with live FX` comment. Rates drift immediately.
**Impact:** Display prices diverge from market reality; potential mispricing if ever used in real checkout.

---
## [FIXED] BUG-043: Currency rounding inconsistent between EGP and USD/EUR

> **Resolution:** Removed the manual `Math.round` pre-rounding step in `format()` and let `toLocaleString` handle rounding in a single step via `minimumFractionDigits`/`maximumFractionDigits` (0 for EGP, 2 for USD/EUR). Eliminates double-rounding and makes precision handling uniform across currencies. Displayed output is unchanged.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/currency.tsx:69`
**Description:** EGP uses `Math.round(converted)` (integer) while USD/EUR uses `Math.round(converted * 100) / 100` (2dp). Cart totals summed in one currency and compared in another can disagree by cents.
**Evidence:**
```tsx
const rounded = cur === "EGP" ? Math.round(converted) : Math.round(converted * 100) / 100;
```

---
## [FIXED] BUG-044: `averageRating` treats missing reviews as 0 for sort purposes

> **Resolution:** Changed `averageRating` return type from `number` to `number | null`, returning `null` when a product has no reviews. Future sort consumers can now distinguish unreviewed items from 0-rated items. Updated the two display call sites (`ProductTile`, `ProductDetail`) to guard on `rating !== null`, preserving identical display output.
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/products.ts:44-46`
**Description:** Returns 0 when no reviews exist — downstream sort-by-rating ranks unreviewed items equal to genuinely 0-rated items.
**Impact:** New products sink to the bottom of rating sorts.
**Evidence:**
```tsx
if (!p.reviews || p.reviews.length === 0) return 0;
```

---
## [FALSE_POSITIVE] BUG-045: Free-shipping threshold boundary messaging

> **Resolution:** Not a bug — the conditional order is already correct. At the exact threshold (`subtotal === 3000`), `remaining = 0`, so line 59's `remaining > 0` check is false and renders "Free shipping unlocked" (not "Add EGP 0 for free shipping"). Line 150's `remaining === 0` correctly renders "FREE". The "Add EGP 0..." copy would only appear if the condition were `>=` rather than `>`, which it is not.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/cart/page.tsx:16-17, 149`
**Description:** `remaining = max(0, THRESHOLD - subtotal)` combined with `remaining === 0` check can yield "Add EGP 0 for free shipping" at exactly the threshold if conditional order is wrong.
**Impact:** Misleading copy at threshold boundary.

---
## [FALSE_POSITIVE] BUG-046: Cart page `−` button expects qty to reach 0 but store prevents it

> **Resolution:** Not a bug — the `−` button is `disabled={it.qty <= 1}` (cart/page.tsx:109), so it cannot fire at qty=1 and `setQty(id, 0)` is never called from this path. Removal is handled by a dedicated `Remove` button that calls `remove(id)` directly. Additionally, after BUG-41 the store's `setQty` now removes on `qty <= 0` rather than clamping back to 1, so the UI/store contract is consistent either way.
**Severity:** High
**Category:** Business Logic

**Location:** `src/app/cart/page.tsx:108`
**Description:** Calls `setQty(it.id, it.qty - 1)` which at qty=1 becomes `setQty(id, 0)` but the store clamps back to 1. Item never removed via decrement.
**Impact:** Broken UX contract between cart UI and store.

---
## [FIXED] BUG-047: Checkout order-number generator uses `Math.random` — collision-prone

> **Resolution:** Replaced the 4-digit random suffix with a millisecond-precision base36 timestamp plus a 3-char base36 random tail: `UG-YY-<ts36>-<rand36>`. Near-unique per order (timestamp provides ms resolution; 46656 random suffix handles same-ms collisions). Also updated the orders-lookup regex in `src/app/orders/page.tsx` and the placeholder to accept the new format, keeping producer and consumer in sync.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:29-32`
**Description:** Only 9,000 possible values per year (`1000-9999`); birthday-paradox collisions likely within a few hundred orders.
**Impact:** Order lookup could resolve to the wrong order.
**Evidence:**
```tsx
const rand = Math.floor(Math.random() * 9000 + 1000);
```

---
## [FIXED] BUG-048: Order-number regex accepts implausible years

> **Resolution:** Tightened the orders-lookup regex year segment from `\d{2}` to `2\d`, so only years 2020–2029 are accepted. Rejects `UG-99-*`, `UG-19-*`, etc. Template-buyers operating past 2029 will need to widen this (alongside the other `TODO(template-buyer)` markers).
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:31` (and `orders/page.tsx` lookup)
**Description:** `^UG-\d{2}-\d{4}$` allows `UG-99-XXXX`. No bounding of year segment.
**Impact:** Mock lookup accepts future/impossible orders.

---
## [SKIPPED] BUG-049: Shipping fee recomputed from live subtotal — can change between pages

> **Resolution:** Skipped — the proper fix is server-side order-freezing at submission (exactly what the existing `TODO(template-buyer)` at checkout/page.tsx:114-116 describes for the real payment integration). A client-side "snapshot subtotal on mount" workaround creates the inverse bug (charging for items no longer in the cart), which is worse than the original. The checkout page has no cart-modification UI, so the risk surface is limited to multi-tab edits. Flagged for the template-buyer's real payment/order-store implementation.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:104`
**Description:** `shipFee` is derived from current `subtotal` each render. If the user removes an item mid-checkout, fee changes silently.
**Impact:** Displayed total at confirmation may differ from cart-page promise.

---
## [FIXED] BUG-050: Checkout autofill excludes email field

> **Resolution:** Removed the explicit `{ email: _e, ...address }` destructure and now persist the full form (including email) to localStorage. `autofill()` already spreads all saved keys back into the form state, so returning customers' email now prefills alongside the shipping address. No comment previously justified the exclusion, and email is the same privacy tier as the address fields already being stored.
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:118-120`
**Description:** `const { email: _e, ...address } = form;` — email intentionally dropped from localStorage, so returning customers must retype it.
**Impact:** Friction on return visits.

---
## [FIXED] BUG-051: Orders mock lookup seed can collide for anagrams

> **Resolution:** Replaced the char-code sum with an FNV-1a hash (32-bit, via `Math.imul`), so position contributes to the seed — `"UG-26-ABC"` and `"UG-26-CBA"` now hash to different values. `Math.abs` normalizes to non-negative for the downstream modulo operations. All existing consumers (stageIdx, placedDays, trackingId, itemCount) work unchanged.
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/orders/page.tsx:35`
**Description:** `trimmed.split("").reduce((s,c) => s + c.charCodeAt(0), 0)` sums char codes — any permutation of the same order number produces the same seed/lookup result.
**Impact:** Typos where characters are transposed still "find" an order.

---
## [FIXED] BUG-052: Shop landing uses `.find` for signature — extras dropped

> **Resolution:** Changed `products.find((p) => p.signature)` to `filter(...)` and renamed the prop from `signature?: Product` to `signatures: Product[]` on `ShopGrid`. The signature section now maps over all signature products, each with its own `ProductTile` and caption. Visibility gating (filter/search) is unchanged; single-signature catalogs render identically to before.
**Severity:** High
**Category:** Business Logic

**Location:** `src/app/shop/page.tsx:8-9`
**Description:** `products.find((p) => p.signature)` returns only the first signature product. The rest are filtered out of the regular grid as well (`!p.signature`), so multiple signatures become invisible.
**Impact:** Product catalog items disappear from the shop page.
**Evidence:**
```tsx
const signature = products.find((p) => p.signature);
const regular = products.filter((p) => !p.signature);
```

---
## [FALSE_POSITIVE] BUG-053: ProductDetail gallery dedupe via `Set` — relies on insertion order

> **Resolution:** Not a bug — line 22's ternary `product.gallery && product.gallery.length > 0 ? product.gallery : [product.image]` makes gallery entries and the fallback image mutually exclusive inputs to the `Set`. There is no code path where `product.image` gets placed before gallery entries, so the "fallback preempting real gallery" concern cannot materialize. ES spec also guarantees `Set` insertion-order iteration.
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:23`
**Description:** `Array.from(new Set(g))` — ES spec does preserve insertion order, but the surrounding logic doesn't guard against the fallback image preempting the real gallery.
**Impact:** Minor — fallback image could appear first if `product.image` is duplicated before gallery entries.

---
## [FIXED] BUG-054: ProductDetail fallback sizes only applies to nullish, not empty array

> **Resolution:** Replaced the `product.sizes ?? [...]` nullish-coalesce with a length check: `product.sizes && product.sizes.length > 0 ? product.sizes : [...]`. Products authored with an empty `sizes: []` now fall back to the default S/M/L/XL list instead of rendering zero selectable sizes.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:25`
**Description:** `const sizes = product.sizes ?? ["S","M","L","XL"];` — a product with `sizes: []` bypasses the fallback and yields zero selectable sizes.
**Impact:** Size selector renders empty; add-to-cart broken.

---
## [FALSE_POSITIVE] BUG-055: Related products slice(0, 4) with no minimum check

> **Resolution:** Not a bug — the entire "more pieces" section (heading and grid) is already gated by `{related.length > 0 && (...)}` at ProductDetail.tsx:381. When the catalog has fewer than 2 items, `related` is empty and nothing renders. The bug report's cited line 316 points to the size-error message, not the related-products heading.
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:46, 316`
**Description:** If catalog has <2 items, `related` is empty but the "more pieces" heading still renders.
**Impact:** Orphan heading with no content.

---
## [FIXED] BUG-056: Size-error state persists across product navigation

> **Resolution:** Extended the existing `useEffect(() => { recordView(...) }, [product.id])` to also reset the per-product UI state on product change: `showSizeError`, `size`, `qty`, `activeIdx`, `justAdded`. Navigating between products via the related-products grid no longer carries stale error messages, selected sizes, quantities, or gallery positions from the previous product.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:30`
**Description:** `showSizeError` is client state not reset when the page's `product` changes. Navigating via related-products while error is visible carries it to the new product.
**Impact:** False error message on freshly navigated product.

---
## [FIXED] BUG-057: Wishlist "add to cart" silently picks the first size

> **Resolution:** Added a per-item size `<select>` (with sr-only `<label>`) next to each Add-to-cart button in `src/app/wishlist/page.tsx`. Selection defaults to the product's first size (or S/M/L/XL fallback when `sizes` is empty/undefined) and is tracked in a `sizeById` state map. The button now reads the current selection, so users always confirm the size explicitly before the item is added.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/wishlist/page.tsx:62`
**Description:** `const defaultSize = p.sizes?.[0] ?? "M";` — no user confirmation of size when adding from wishlist.
**Impact:** Users receive wrong size; return/exchange friction.
**Evidence:**
```tsx
const defaultSize = p.sizes?.[0] ?? "M";
add(p, defaultSize, 1);
```

---
## [FALSE_POSITIVE] BUG-058: Cart add does not key on size — same id+different size collapses

> **Resolution:** Not a bug — `src/lib/cart.tsx` already composites size into the line ID via `lineId = \`${product.id}__${size}\``, and lookups match on that composite. Adding product X in sizes M and L produces two separate cart lines. Only same-product+same-size re-adds increment an existing line, which is the intended behavior.
**Severity:** High
**Category:** Business Logic

**Location:** `src/lib/cart.tsx` (add/findExisting logic)
**Description:** If the existing-item lookup matches by `id` only (not by `id+size`), adding the same product in two sizes increments one row instead of creating two.
**Impact:** Users cannot have the same product in multiple sizes in the cart simultaneously.

---
## [FIXED] BUG-059: localStorage hydration race on cart/wishlist/recentlyViewed

> **Resolution:** Cart and wishlist hydration effects now use functional `setState` updaters that merge the saved data with any items already in memory from a pre-hydration interaction (cart merges by lineId and sums qty, clamped to 99; wishlist unions via `Set`). If the user clicks Add before the hydration read completes, their action is preserved instead of being overwritten. `recentlyViewed` needed no change — it reads fresh from storage on every `recordView` call and therefore can't drop in-flight writes.
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/cart.tsx`, `src/lib/wishlist.tsx`, `src/lib/recentlyViewed.tsx`
**Description:** Initial state is empty on SSR; `useEffect` then reads localStorage. If the user clicks "add" before the read completes, their local history is overwritten with the post-click state.
**Impact:** Persisted cart/wishlist silently erased on rapid first-interaction.

---
## [FALSE_POSITIVE] BUG-060: Newsletter email validation uses naive regex
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/Newsletter.tsx` (validation)
**Description:** Typical `/.+@.+\..+/` style regex accepts strings like `a@b.c` and rejects legitimate addresses containing `+`/unicode.
**Impact:** False positives/negatives on email capture.

> **Resolution:** The actual regex on line 14 is `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, which is stricter than the example cited. It accepts `+` addresses (e.g. `user+tag@example.com`) and unicode chars (neither is whitespace nor `@`), so the two specific failure modes named in the bug don't apply. Accepting `a@b.c` is a deliberate leniency — the input also uses `type="email"` for browser-native validation, and authoritative validation is delegated to the mailing-list provider (TODO(template-buyer) marker at line 19). No change.

---
## [FALSE_POSITIVE] BUG-061: Reviews filter+sort not stable across identical timestamps
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/Reviews.tsx`
**Description:** Sort-by-recent uses raw date compare without tiebreaker; identical timestamps reorder across renders.
**Impact:** Visual flicker of review order.

> **Resolution:** Not a bug — `Reviews.tsx` has no sort feature at all. The only interactive control is a star-rating filter, and `Array.prototype.filter` preserves source order. No `.sort()`, no Date parsing, no comparator to stabilize. No change.

---
## [FALSE_POSITIVE] BUG-062: ShopGrid text search case-sensitivity assumed
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx` (filter)
**Description:** If comparing strings without `.toLowerCase()` on both sides, partial matches miss mixed-case queries (e.g., "Hood" vs "hood").

> **Resolution:** Not a bug — ShopGrid already lowercases both sides. Query is normalized via `query.trim().toLowerCase()` (line 51), then compared against `p.name.toLowerCase()` and `(p.description ?? "").toLowerCase()` (lines 56–57). Mixed-case queries match correctly. No change.
**Impact:** Users miss products they'd otherwise find.

---
## [FALSE_POSITIVE] BUG-063: Currency change does not re-price items already in cart
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/cart.tsx` × `src/lib/currency.tsx`
**Description:** Cart stores priceEGP at add time; currency switch re-renders with new formatter, but if any snapshot (checkout summary, order confirmation) was persisted in old currency, totals diverge.
**Impact:** Inconsistent totals across pages if currency changes mid-flow.

> **Resolution:** Not a bug — no price snapshot is ever persisted in a converted currency. Cart stores canonical `priceEGP`; every display (cart, drawer, checkout, product pages) calls `format(priceEGP * qty)` which reconverts live from EGP using the current rate. Checkout only persists the address form, orders page shows no prices, and there is no order-confirmation route. Currency switches re-price everything consistently.

---
## [FALSE_POSITIVE] BUG-064: `recordView` dispatches window event with no SSR guard at call site
**Severity:** High
**Category:** API & Data

**Location:** `src/lib/recentlyViewed.tsx:42`
**Description:** `recordView` is called from `ProductDetail` during render paths. The internal `typeof window` guard returns early, but callers that wrap state around the result (or react to the dispatched event) assume client execution.
**Impact:** Silent no-op during SSG — "recently viewed" can miss entries the first render.

> **Resolution:** Not a bug — the only caller (`ProductDetail.tsx:43-50`) invokes `recordView` inside `useEffect`, which only runs client-side after hydration. It is never called during render or SSG. The `typeof window` guard in `recentlyViewed.tsx` is belt-and-suspenders. No callers read a return value (function returns `void`).
**Evidence:**
```tsx
window.dispatchEvent(new CustomEvent("underground:recently-viewed-changed"));
```

---
## [FALSE_POSITIVE] BUG-065: Cart persistence effect writes to `window.localStorage` without SSR check
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx:62-69`
**Description:** Effect gates on `hydrated` flag but not on `typeof window`. Under certain Next.js hydration edge cases the effect can fire before window is defined.
**Impact:** Potential ReferenceError on exotic runtimes.

> **Resolution:** Not a bug — React's `useEffect` never runs on the server. It is scheduled after commit, which only happens in the browser. `window.localStorage` inside any `useEffect` in a `"use client"` module is safe; no extra `typeof window` guard is needed.

---
## [FIXED] BUG-066: Cart localStorage write fails silently when quota exceeded
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx:64-68`
**Description:** `try { localStorage.setItem ... } catch {}` — quota/privacy-mode failures produce no user-visible signal.
**Impact:** User thinks cart is persisted; a reload drops it.

> **Resolution:** Replaced the empty `catch {}` with `console.warn("[cart] persistence failed — cart will not survive reload", err)` so quota/privacy-mode failures are visible in the console and captured by any error-monitoring integration a template buyer wires up. A user-facing toast was deliberately not added — it's UI scope for an edge case, and the in-memory cart continues to work for the session.
**Evidence:**
```tsx
try { window.localStorage.setItem(STORAGE_KEY, JSON.stringify(items)); } catch { /* silent */ }
```

---
## [FALSE_POSITIVE] BUG-067: Wishlist `useMemo` includes its own callbacks in deps
**Severity:** High
**Category:** API & Data

**Location:** `src/lib/wishlist.tsx:70`
**Description:** The context value memo depends on `has, toggle, add, remove, clear` — all functions recreated per render. Memo is effectively useless and retriggers every consumer.
**Impact:** Excess re-renders through the entire wishlist consumer tree.

> **Resolution:** Not a bug — all five callbacks are `useCallback`-wrapped. `add`/`remove`/`toggle`/`clear` have empty deps (stable), and `has` has `[ids]` (changes in lockstep with `ids`). The `useMemo` effectively retriggers only when `ids` changes — identical to the intended behavior. The callbacks are not "recreated per render."
**Evidence:**
```tsx
useMemo(() => ({ ids, has, toggle, add, remove, clear, count: ids.length }),
  [ids, has, toggle, add, remove, clear]);
```

---
## [FIXED] BUG-068: Currency `setCurrency` writes to localStorage before hydration
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/currency.tsx:52-59`
**Description:** `setCurrency` can be called before the hydration effect has read the prior value, overwriting persisted state with a default.
**Impact:** Persisted currency preference can be clobbered on fast interaction.

> **Resolution:** Moved the localStorage write out of `setCurrency` into a hydration-gated `useEffect([currency, hydrated])` that only runs when `hydrated === true`. Matches the pattern in `cart.tsx` and `wishlist.tsx`. `setCurrency` now only updates React state; persistence happens in the effect once hydration has read the prior value.

---
## [FIXED] BUG-069: Checkout submit has no error path
**Severity:** High
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** `await new Promise(r => setTimeout(r, 800))` always resolves. No try/catch; when swapped for a real API this handler will throw uncaught promise rejections.
**Impact:** Users stuck on "Placing order…" forever; no error UI.

> **Resolution:** Wrapped the submit body in try/catch/finally. Added a `submitError` state and a `role="alert"` message above the submit button. `setBusy(false)` now runs in `finally`, so the button re-enables whether the call succeeds or fails. Template buyers swapping in Stripe/Paymob/Fawry now get user-visible failure handling for free.
**Evidence:**
```tsx
setBusy(true);
await new Promise((r) => setTimeout(r, 800));
const orderNumber = generateOrderNumber();
```

---
## [FALSE_POSITIVE] BUG-070: Checkout submit has no AbortController / timeout
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** Component can unmount mid-submit; pending promise still calls `setDone`/`setBusy` on an unmounted component.
**Impact:** React "set state on unmounted component" warning; potential memory leak.

> **Resolution:** Not a bug for the current code — React 18+ silently discards setState on unmounted components (the warning was removed), and the mock `setTimeout(800)` has no cancellable resource. Adding an AbortController to a timer stub is over-engineering; the real abort contract belongs with whatever payment provider (Stripe/Paymob/Fawry) the template buyer wires up.

---
## [FIXED] BUG-071: Checkout double-submit protected only by `busy` state flag
**Severity:** High
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** No `useRef` or ref-backed in-flight guard. A rapid Enter-key burst or programmatic submit can re-enter before React commits the `busy=true` render.
**Impact:** Duplicate order numbers; duplicate localStorage writes.

> **Resolution:** Added a `submitInFlight = useRef(false)` guard at the top of `handleSubmit`. The ref flips synchronously (no React commit latency), so rapid Enter presses or programmatic resubmits during the same tick early-return instead of re-entering. Ref is cleared in `finally` alongside `busy`.

---
## [FIXED] BUG-072: Orders lookup has no request cancellation
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/orders/page.tsx:57-70`
**Description:** Multiple "Track" clicks stack promises; the last to resolve wins regardless of submit order.
**Impact:** Wrong result displayed on rapid re-submits.

> **Resolution:** Added two refs: `lookupInFlight` blocks synchronous re-entry before React commits `busy=true`, and `lookupSeq` is a monotonic counter — only the most recently issued request writes its result. Both are cleared in `finally`. Also wrapped the body in try/finally so `busy` always clears.

---
## [FIXED] BUG-073: Pre-order submit has no try/catch
**Severity:** High
**Category:** API & Data

**Location:** `src/app/pre-order/page.tsx:47-57`
**Description:** Same pattern as checkout — no error path around the mocked await.
**Impact:** Real API failure would bubble as uncaught rejection.

> **Resolution:** Applied the same try/catch/finally pattern used for BUG-069 (checkout). Added `submitError` state and a `role="alert"` message above the Reserve button. `setBusy(false)` runs in `finally` so the button re-enables whether the POST succeeds or fails.

---
## [FIXED] BUG-074: Contact submit has no try/catch
**Severity:** High
**Category:** API & Data

**Location:** `src/app/contact/page.tsx:38-48`
**Description:** No error handling around async form submission.
**Impact:** Uncaught rejection on real API failure.

> **Resolution:** Applied the same try/catch/finally pattern used for BUGs 069 (checkout) and 073 (pre-order). Added `submitError` state + `role="alert"` message above the Send button. `setBusy(false)` runs in `finally`.

---
## [FIXED] BUG-075: Home hero image hardcoded to external Unsplash URL
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/home/page.tsx:17`
**Description:** Hero image points at `images.unsplash.com/...` with no fallback and no allowlist in `next.config` for production builds.
**Impact:** Hero breaks if Unsplash is unreachable or CDN key expires.

> **Resolution:** Added an `onError` fallback to the hero `<Image>`: on load failure `setHeroSrc("/fallback-garment.svg")` — mirrors the existing pattern in `ProductTile.tsx`. The "no allowlist" part of the bug was factually wrong — `next.config.ts` already includes `images.unsplash.com` in `images.remotePatterns`.
**Evidence:**
```tsx
const HERO_IMAGE = "https://images.unsplash.com/photo-1579725854926-...";
```

---
## [FALSE_POSITIVE] BUG-076: Home parallax media-query listener relies on stale closure
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/home/page.tsx:26-32`
**Description:** `update` captures its render-time `setParallaxEnabled`; fine in React 18 but the effect has no deps tracking the resize handler it ends up installing.
**Impact:** Minor — potential re-subscription edge cases.

> **Resolution:** Not a bug — the effect has empty deps and runs once. `update` closes over React's guaranteed-stable `setParallaxEnabled` setter and the `mql` captured at mount; cleanup removes the same `update` reference. Nothing the handler reads changes between renders, so no stale closure or re-subscription risk exists.

---
## [FALSE_POSITIVE] BUG-077: CartDrawer mutates `document.body.style` without SSR guard
**Severity:** Medium
**Category:** API & Data

**Location:** `src/components/CartDrawer.tsx:21-22`
**Description:** Effect writes `document.body.style.overflow` directly. Effect body runs only client-side in practice, but there's no `typeof document` check for safety.
**Impact:** Fragile under alternative runtimes / react-dom/server integrations.

> **Resolution:** Not a bug — same principle as BUG-065: React's `useEffect` (and its cleanup) only runs after commit in the browser. `document` is guaranteed to exist inside any `useEffect` body in a `"use client"` component. No SSR guard required.

---
## [FALSE_POSITIVE] BUG-078: Cursor `requestAnimationFrame` cleanup only cancels the last scheduled frame
**Severity:** Medium
**Category:** API & Data

**Location:** `src/components/Cursor.tsx:39, 43-44`
**Description:** `raf` is reassigned inside `loop`; cleanup cancels the newest handle only — if a re-render schedules a second RAF before cleanup fires, the old one runs to completion.
**Impact:** Briefly doubled RAF loop across unmount boundary.

> **Resolution:** Not a bug — the RAF chain is sequential: each `loop()` call schedules exactly one next frame, overwriting the prior handle. Only one frame is ever pending at a time. The effect has empty deps so it doesn't re-run; no second chain can be started. Cleanup cancels the single pending handle correctly.

---
## [FIXED] BUG-079: Newsletter submit has no error state and no network handler
**Severity:** Medium
**Category:** API & Data

**Location:** `src/components/Newsletter.tsx`
**Description:** "Subscribe" resolves via setTimeout with no failure branch; when wired to a real ESP API, failures will appear as a permanent "loading" state.
**Impact:** No error path for subscriber capture.

> **Resolution:** Wrapped the subscribe body in try/catch. Added an `errorMsg` state so the existing `err` state can display either "Invalid email" (validation) or "Couldn't subscribe. Please try again." (network). On failure the state transitions `busy → err`, unsticking the form. Existing onChange handler already clears `err → idle` so users can retry.

---
## [FALSE_POSITIVE] BUG-080: Checkout `done.email` rendered without null check
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:152-154`
**Description:** Success message interpolates `done.email`. If the validator is ever bypassed, renders an empty span.
**Impact:** Empty confirmation line on edge inputs.

> **Resolution:** Not a bug — `done` is typed `{ orderNumber: string; email: string } | null`. The only path that sets `done` runs `validate(form)` first, which rejects empty/invalid emails (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`). `done.email` is non-null and non-empty by construction; the "validator bypass" scenario requires editing source code.

---
## [FIXED] BUG-081: Address persisted to localStorage inside the async submit critical path

> **Resolution:** Reordered the success path in `checkout/page.tsx` so `setDone` + `clear` fire first, then `localStorage.setItem(ADDRESS_KEY, …)` is deferred via `setTimeout(…, 0)`. A slow or full storage can no longer delay the success-screen transition.

**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:117-123`
**Description:** Synchronous `localStorage.setItem` runs before `setBusy(false)`. A slow/full storage blocks the UI transition to the success screen.
**Impact:** Perceived hang on storage-constrained devices.

---
## [FIXED] BUG-082: `JSON.parse` of localStorage state not validated against schema

> **Resolution:** Added an `isCartItem` type guard in `src/lib/cart.tsx` that validates each entry's fields (id/productId/name/image/size strings, finite priceEGP, qty > 0) and filters malformed items out of the loader. The other three loaders listed (wishlist, recentlyViewed, currency) were already validating their shapes — no changes needed there.

**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx`, `src/lib/wishlist.tsx`, `src/lib/recentlyViewed.tsx`, `src/lib/currency.tsx`
**Description:** Each loader does `JSON.parse(raw)` but does not validate the result shape. A user tampering with localStorage (or an old schema version) yields undefined fields accessed downstream.
**Impact:** Runtime crashes; silent schema drift; XSS vectors if malicious values reach React props.

---
## [FALSE_POSITIVE] BUG-083: ShopGrid filter memo rebuilds when parent passes a fresh `regular` array

> **Resolution:** Not a bug in the current codebase. `ShopGrid`'s only caller is `src/app/shop/page.tsx`, a server component that renders once per request — client-side state changes inside `ShopGrid` don't re-invoke the parent, so the `regular` array reference is stable for the component's lifetime and the memo hits correctly.

**Severity:** Medium
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:50-76`
**Description:** `useMemo([regular, filter, query, sort, hideSoldOut])` — if the parent re-derives `regular` inline (via `.filter`) on each render, the memo never hits.
**Impact:** Expensive sort/filter runs on every ancestor render.

---
## [FIXED] BUG-084: ProductDetail gallery memo keyed on full `product` object

> **Resolution:** Added `safeIdx = Math.min(activeIdx, gallery.length - 1)` in `ProductDetail.tsx` and use it for all render-time reads (image src/alt/priority, `AnimatePresence` key, thumbnail `aria-selected`/`tabIndex`/border). The existing `useEffect` on `[product.id]` still resets `activeIdx` to 0 on commit; the clamp protects the intermediate render after a client-side navigation from a long gallery to a short one, which previously could produce `gallery[3] === undefined` and crash `<Image>`.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx:21-24`
**Description:** `useMemo(..., [product])` recomputes whenever a new `product` object identity arrives, even when gallery content is unchanged. `activeIdx` state is not clamped to new length.
**Impact:** Index-out-of-bounds when product changes to one with fewer images.
**Evidence:**
```tsx
const gallery = useMemo(() => { ... }, [product]);
// activeIdx not reset/clamped
```

---
## [FALSE_POSITIVE] BUG-085: ProductDetail `handleAdd` schedules setTimeout with no cleanup

> **Resolution:** The stated impact ("setState on unmounted component; warning in dev") doesn't occur. This project is on React 19, and React 18+ removed the unmounted-setState warning — setState on an unmounted component is a silent no-op. The 1.4s timer holds only a small closure that garbage-collects on its own; there's no leak or visible symptom to fix.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx:55`
**Description:** `setTimeout(() => setJustAdded(false), 1400)` is not cleared on unmount.
**Impact:** setState on unmounted component; warning in dev.
**Evidence:**
```tsx
setJustAdded(true);
setTimeout(() => setJustAdded(false), 1400);
```

---
## [FIXED] BUG-086: ProductDetail `related` computed inline without memoization

> **Resolution:** Wrapped `products.filter(...).slice(0, 4)` in `useMemo` keyed on `product.id` in `ProductDetail.tsx`. The filter now runs once per product instead of every render, and `related` has stable identity within a page view.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx:46`
**Description:** `products.filter(...).slice(0, 4)` runs every render.
**Impact:** Minor perf cost; new array identity each render propagates to children.

---
## [FIXED] BUG-087: RecentlyViewedStrip `items` pipeline not memoized

> **Resolution:** Wrapped the `ids.filter().map().filter().slice(0, 6)` chain in `useMemo` keyed on `[ids, excludeId]` in `RecentlyViewedStrip.tsx`. The pipeline now only runs when recently-viewed state actually changes, not on every ancestor render (currency flips, theme toggles, etc.).

**Severity:** Low
**Category:** State Management

**Location:** `src/components/RecentlyViewedStrip.tsx:19-23`
**Description:** `ids.filter().map().filter().slice()` each render.
**Impact:** Needless recompute on every parent render.

---
## [FALSE_POSITIVE] BUG-088: CartDrawer Escape-key effect re-subscribes when `closeCart` identity changes

> **Resolution:** `closeCart` is defined in `cart.tsx` as `useCallback(() => setIsOpen(false), [])` — empty deps, so React guarantees stable identity for the provider's lifetime. The CartDrawer Escape-key effect only re-subscribes when `isOpen` toggles, which is the intended behavior. No thrashing occurs.

**Severity:** Medium
**Category:** State Management

**Location:** `src/components/CartDrawer.tsx:28-35`
**Description:** Effect deps `[isOpen, closeCart]`. If the cart context ever returns a fresh `closeCart` reference per render, this handler rebinds on every render.
**Impact:** Thrashed `addEventListener` / `removeEventListener` calls.

---
## [FALSE_POSITIVE] BUG-089: Newsletter async submit lacks mounted-flag guard

> **Resolution:** The stated impact ("setState-on-unmounted warning; potential leak") doesn't occur. This project is on React 19, which silently ignores setState on unmounted components (warning was removed in React 18). The 500ms timer closure holds only local state setters and GCs naturally — no subscriptions, no leak.

**Severity:** High
**Category:** State Management

**Location:** `src/components/Newsletter.tsx:57`
**Description:** `setState("ok")` called after 500ms `await` without checking whether the component is still mounted.
**Impact:** setState-on-unmounted warning; potential leak.
**Evidence:**
```tsx
setState("busy");
await new Promise((r) => setTimeout(r, 500));
setState("ok");
```

---
## [FALSE_POSITIVE] BUG-090: Orders page async submit lacks mounted guard

> **Resolution:** React 19 silently no-ops setState on unmounted components (warning was removed in React 18). The async closure in `orders/page.tsx` `handleSubmit` holds only local state setters with no external subscriptions. The separate concern about stale results from overlapping submits is already handled by the existing `lookupSeq` monotonic counter added in BUG-072.

**Severity:** High
**Category:** State Management

**Location:** `src/app/orders/page.tsx:57-70`
**Description:** Sets `setBusy`, `setError`, `setOrder` after await with no unmount check.
**Impact:** State-update-on-unmounted component.

---
## [FALSE_POSITIVE] BUG-091: Pre-order submit lacks mounted guard

> **Resolution:** React 19 silently no-ops setState on unmounted components (warning removed in React 18). The pre-order submit closure holds only local state setters; no external subscriptions to leak. The try/catch/finally error handling added in BUG-073 already covers the real correctness concern.

**Severity:** High
**Category:** State Management

**Location:** `src/app/pre-order/page.tsx:47-57`
**Description:** Same pattern — sets `busy`/`submitted` after await without check.

---
## [FALSE_POSITIVE] BUG-092: Contact submit lacks mounted guard

> **Resolution:** React 19 silently no-ops setState on unmounted components (warning removed in React 18). The contact submit closure holds only local state setters; no external subscriptions to leak. Error handling already in place from BUG-074.

**Severity:** High
**Category:** State Management

**Location:** `src/app/contact/page.tsx:38-48`
**Description:** Same pattern as above.

---
## [FALSE_POSITIVE] BUG-093: Checkout submit lacks mounted guard

> **Resolution:** Two claims, both non-issues. (1) React 19 silently no-ops setState on unmounted components — no warning exists. (2) Clearing the cart after successful submit is intentional, not a bug: `CartProvider` lives at the app root so `clear()` works regardless of checkout unmount, and a successful order *should* clear the cart. Production webhook-driven clearing is already flagged with a `TODO(template-buyer)` in the file.

**Severity:** High
**Category:** State Management

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** `setDone`, `setBusy(false)` called after await with no mount check; also clears cart — if user navigated away, cart is cleared anyway.
**Impact:** Cart cleared even when navigating away mid-submit.

---
## [FALSE_POSITIVE] BUG-094: Checkout `autofill()` race — reads localStorage before hydration flag

> **Resolution:** `CheckoutPage` early-returns a placeholder shell (`<main className="min-h-[40vh]" />`) while `!mounted`, so the form UI and the autofill button don't exist before the mount effect commits. There is no render path where `autofill()` is reachable before hydration, so the race cannot occur.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/checkout/page.tsx:78-86 / autofill()`
**Description:** `autofill` is callable from UI before the `mounted` effect sets the hydration flag; reads localStorage but other derived UI assumes mounted state.
**Impact:** Possible hydration mismatch if autofill applied before hydration completes.

---
## [FALSE_POSITIVE] BUG-095: Home parallax `update` closure captures initial `setParallaxEnabled`

> **Resolution:** React documents `useState` setter identity as stable across renders — it's a guaranteed API, not a fragile pattern. `mql.addEventListener("change", …)` has been supported in Safari since 14 (2020); Next.js 15 / React 19 already drop older browsers, so the `addListener` legacy shim is unnecessary for this project.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/home/page.tsx:26-32`
**Description:** Effect deps `[]` — `update` listener references initial setter. (setter identity is stable in React but the pattern relies on it; legacy Safari addListener path not handled either.)
**Impact:** Fragile in older engines; `mql.addEventListener` is the modern but narrow API.

---
## [FALSE_POSITIVE] BUG-096: Wishlist `count` derived inside memo that lists its own callbacks as deps

> **Resolution:** The claim that the memo "rebuilds on each render regardless" is wrong. In `wishlist.tsx`, `add`/`remove`/`toggle`/`clear` are `useCallback([])` (permanently stable), and `has` is `useCallback([ids])` — it changes only when `ids` changes. So the value memo invalidates iff `ids` changes, which is exactly when `count: ids.length` could change. Optimal behavior.

**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/wishlist.tsx:70-71`
**Description:** `count: ids.length` is computed inside the memo; memo deps include function refs already tied to `ids`, so memo rebuilds on each render regardless.
**Impact:** Effective memo defeat — every consumer sees a new value object each render.

---
## [FALSE_POSITIVE] BUG-097: `recordView` fires on every product.id remount with no dedupe window

> **Resolution:** Each `recordView` call represents a meaningful state change (reordering the visited id to the front), and the dispatched event correctly reflects that. The only truly redundant case is two back-to-back visits of the same id — rare in production, and dev Strict Mode's double-mount produces at most one spurious event per navigation. Listener work is trivial (localStorage parse + filter of ≤8 items). Not worth adding a dedupe window.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx:42-44` + `src/lib/recentlyViewed.tsx`
**Description:** Rapid back/forward or remount loops push duplicate records into the list (dedupe is by `id` but the dispatched event fires each time).
**Impact:** Excess event dispatches / listener churn.

---
## [FALSE_POSITIVE] BUG-098: Cart `items` array passed to consumers is the raw state array — mutation-prone

> **Resolution:** Theoretical concern, not an actual bug. A grep of `src/` for mutating array methods on `items` (`sort`/`push`/`splice`/`reverse`/`pop`/`shift`/`unshift`/`fill`/`copyWithin`) returns zero hits — every consumer uses `reduce`/`find`/`filter`/`map`. Exposing raw state is idiomatic React; `Object.freeze` would add perf cost with no corresponding defect to prevent.

**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/cart.tsx` (context value)
**Description:** Context exposes `items` directly. A careless consumer doing `items.sort()` would mutate state. No `Object.freeze` / defensive copy.
**Impact:** Accidental direct mutation breaks React reconciliation.

---
## [FALSE_POSITIVE] BUG-099: Checkout `form` is shallow-merged — nested fields could be mutated

> **Resolution:** Speculative future-proofing, not a current bug. All 9 form fields (`email`, `firstName`, `lastName`, `address`, `address2`, `city`, `country`, `postal`, `phone`) are plain strings — shallow merge via `{ ...prev, [key]: value }` is correct. No nested objects exist. Per project guidelines, we don't design for hypothetical future schema changes.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/checkout/page.tsx` (form state + setForm pattern)
**Description:** If any field holds an object (e.g., address subfields in future), shallow setForm merges won't deep-clone.
**Impact:** Latent bug once fields grow nested.

---
## [FALSE_POSITIVE] BUG-100: Currency context value not memoized — every consumer re-renders on each provider render

> **Resolution:** The claim is factually wrong. `currency.tsx` already wraps the provider value in `useMemo(..., [currency, hydrated])` — consumers only re-render when `currency` or `hydrated` changes. No fix needed.

**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/currency.tsx` (provider value)
**Description:** If the provider's `value={{ currency, setCurrency, format, convert }}` object is created inline without `useMemo`, every provider render creates a new reference.
**Impact:** Every component using `useCurrency` re-renders on unrelated parent renders.

---
## BUG-101: No React error boundary anywhere in the app
**Severity:** High
**Category:** Error Handling

**Location:** `src/app/` (no `error.tsx` / `global-error.tsx` files exist)
**Description:** Next.js app router supports `error.tsx` per route segment; none are present. Any rendering error crashes the whole tree to the default dev overlay in dev, and shows a blank page in prod.
**Impact:** Any runtime error in cart/checkout/product anywhere = blank page with no recovery.

---
## BUG-102: No `not-found.tsx` for custom 404 UI
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/app/`
**Description:** `notFound()` is called in `src/app/shop/[id]/page.tsx:32` but no custom 404 page exists — user lands on Next's generic 404.
**Impact:** Off-brand 404 experience, no nav back to shop.

---
## BUG-103: No `loading.tsx` segments
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/`
**Description:** No route-level loading UI; transitions rely solely on the `PageTransition` client animation.
**Impact:** Cold navigations to dynamic routes show blank screen briefly.

---
## BUG-104: `currency.tsx` catch blocks swallow errors silently
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/currency.tsx:43-46, 54-56`
**Description:** Two `try { ... } catch {}` blocks with no logging and no telemetry.
**Impact:** Quota/security errors around localStorage are invisible in production.
**Evidence:**
```tsx
try { ... } catch { /* nothing */ }
```

---
## BUG-105: `cart.tsx` `loadInitial` swallows JSON.parse errors
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:42-47`
**Description:** Bad JSON -> empty state returned, with no user notification or log.
**Impact:** Users silently lose cart contents on any schema/corruption event.

---
## BUG-106: `cart.tsx` persistence catch is empty
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:64-66`
**Description:** `localStorage.setItem` wrapped in bare `catch {}`. Quota-exceeded errors vanish.
**Impact:** Persistence silently disabled; cart lost on reload.

---
## BUG-107: `wishlist.tsx` loader catch is empty
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/wishlist.tsx:29-34, 50-52`
**Description:** Both load and save paths swallow.
**Impact:** Wishlist silently drops on corruption.

---
## BUG-108: `recentlyViewed.tsx` load/save catch empty
**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/recentlyViewed.tsx:10-23`
**Description:** Swallowed catches.
**Impact:** Recently-viewed breakage invisible.

---
## BUG-109: Checkout `autofill()` / save-address catch blocks swallow errors
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx:80-95, 117-121`
**Description:** Three empty catch blocks, no logging. Users get no feedback if autofill failed or save failed.
**Impact:** "Why didn't autofill work?" — no diagnostic trail.

---
## BUG-110: Checkout async submit has no try/catch around awaited mock
**Severity:** High
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** Unhandled rejection path — if the awaited promise ever rejects (when swapped for fetch), the entire `handleSubmit` returns a rejected Promise which React warns about in dev and leaves busy=true.
**Impact:** Users stuck on "Placing order…" with no error UI.

---
## BUG-111: Orders page async submit has no try/catch
**Severity:** High
**Category:** Error Handling

**Location:** `src/app/orders/page.tsx:57-70`
**Description:** Same unhandled-rejection gap.
**Impact:** Stuck busy state on failure.

---
## BUG-112: Pre-order submit has no try/catch
**Severity:** High
**Category:** Error Handling

**Location:** `src/app/pre-order/page.tsx:47-57`

---
## BUG-113: Contact submit has no try/catch
**Severity:** High
**Category:** Error Handling

**Location:** `src/app/contact/page.tsx:38-48`

---
## BUG-114: Newsletter submit has no try/catch + no error UI
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/components/Newsletter.tsx`
**Description:** The only failure mode handled is client-side regex validation. Network-layer failure (future) has no surface.
**Impact:** Subscribers think they signed up; they didn't.

---
## BUG-115: `Logo.tsx` `fs.readFileSync` throws unhandled on missing file
**Severity:** High
**Category:** Error Handling

**Location:** `src/components/Logo.tsx` (module-load readFileSync)
**Description:** No try/catch; any I/O error crashes the build/request.
**Impact:** Entire site fails to build/render if `public/logo.svg` is missing or unreadable.

---
## BUG-116: `notFound()` used but no telemetry / breadcrumb
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/shop/[id]/page.tsx:32`
**Description:** On bad product id we call `notFound()` without logging. Can't tell legitimate typos from bad links.
**Impact:** No diagnostic signal for broken product links.

---
## BUG-117: No global `window.onerror` / `unhandledrejection` handler
**Severity:** Medium
**Category:** Error Handling

**Location:** Project-wide (no handler installed)
**Description:** No client-side hook to report uncaught errors/promises to a logger.
**Impact:** Silent production failures; no signal to the team.

---
## BUG-118: Image `<Image>` usages lack `onError` fallback handling
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/components/ProductTile.tsx`, `src/app/shop/[id]/ProductDetail.tsx`, `src/app/cart/page.tsx`, `src/app/wishlist/page.tsx`
**Description:** Broken product image paths won't swap to `/fallback-garment.svg` — there's no `onError={() => setSrc(fallback)}`.
**Impact:** Broken image icons instead of designed fallback.

---
## BUG-119: Form validation errors not announced via `role="alert"` / `aria-live`
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx`, `src/app/contact/page.tsx`, `src/app/pre-order/page.tsx`, `src/app/orders/page.tsx`, `src/components/Newsletter.tsx`
**Description:** Error text renders but is not announced to assistive tech.
**Impact:** Screen readers don't surface error feedback.

---
## BUG-120: Generic error rendering — no distinction between user error vs system error
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/orders/page.tsx` (single `error` string)
**Description:** Any lookup failure shows the same message — user can't tell the difference between "typo in order #" and "server broken".
**Impact:** Unhelpful diagnostics; increased support load.

---
## BUG-121: `framer-motion` imported as named exports in 7 files — full bundle shipped
**Severity:** Medium
**Category:** Performance

**Location:** `CartDrawer.tsx:5`, `home/page.tsx:5`, `BarButton.tsx:4`, `PageTransition.tsx:3`, `lookbook/page.tsx:5`, `ProductTile.tsx:5`, `shop/[id]/ProductDetail.tsx:6`
**Description:** `framer-motion` is a ~60KB (min+gzip) dependency pulled into both the landing page and every page via `BarButton` / `PageTransition`. No `LazyMotion` / `m.` component strategy used.
**Impact:** Larger initial JS bundle than necessary; LCP impact on mobile.
**Evidence:**
```tsx
import { motion, AnimatePresence, useReducedMotion } from "framer-motion";
```

---
## BUG-122: `Logo.tsx` performs synchronous `fs.readFileSync` on every request
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/Logo.tsx`
**Description:** Reads `public/logo.svg` at module load. Next.js in dev may re-evaluate per request; even in prod the inlined SVG bloats every page's HTML.
**Impact:** Every page ships the SVG inline; no browser caching of the logo.

---
## BUG-123: ShopGrid filter + sort runs on every keystroke with no debounce
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/ShopGrid.tsx` (search input → `query` state)
**Description:** Text input updates state synchronously; memo recomputes product list on every character. For small catalogs this is fine but scales poorly.
**Impact:** Input lag once catalog grows; unnecessary work on each keystroke.

---
## BUG-124: ProductTile computes seeded rotation in render instead of once per mount
**Severity:** Low
**Category:** Performance

**Location:** `src/components/ProductTile.tsx` + `src/lib/seededRotation.ts`
**Description:** Hash + PRNG runs inline each render. The output is deterministic per-id, but there's no `useMemo([product.id])`.
**Impact:** Trivial CPU burn on every re-render of every tile in the grid.

---
## BUG-125: Reviews sort/filter recomputed every render with no memo
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Reviews.tsx`
**Description:** Sort/filter pipeline runs inline on `reviews` each render.
**Impact:** Minor — scales with review count.

---
## BUG-126: Home page parallax uses `useScroll`/`useTransform` even when reduced-motion is set
**Severity:** Low
**Category:** Performance

**Location:** `src/app/home/page.tsx`
**Description:** `useScroll`/`useTransform` hooks subscribe to scroll events unconditionally; the reduced-motion branch only affects the render, not the subscription.
**Impact:** Needless scroll listener cost on reduced-motion users.

---
## BUG-127: `PageTransition` wraps every route — runs `AnimatePresence` on every nav
**Severity:** Low
**Category:** Performance

**Location:** `src/components/PageTransition.tsx`
**Description:** Every segment swap re-mounts under AnimatePresence, blocking paint until exit completes.
**Impact:** Perceived navigation delay.

---
## BUG-128: Cursor component runs RAF loop unconditionally on mount before media-query check
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Cursor.tsx`
**Description:** If `matchMedia('(hover: hover)')` returns false only after the first paint, RAF has already scheduled.
**Impact:** Brief unnecessary RAF on touch devices.

---
## BUG-129: `Timestamp` runs `setInterval(1000)` globally in nav
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Timestamp.tsx`
**Description:** 1Hz timer mounted on every page via Nav; keeps JS thread hot and prevents tab from idling as deeply.
**Impact:** Minor battery / CPU cost; prevents CPU sleep during idle tabs.

---
## BUG-130: Products JSON bundled into every client page
**Severity:** Medium
**Category:** Performance

**Location:** `src/content/products.json` imported via `src/lib/products.ts`
**Description:** Full catalog JSON is client-side imported by `ShopGrid`, `RecentlyViewedStrip`, `wishlist/page.tsx`, etc. As catalog grows, every page ships all product data.
**Impact:** Bundle growth scales linearly with catalog size; not tree-shakeable.

---
## BUG-131: Gallery images not lazy-loaded beyond above-the-fold
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/shop/[id]/ProductDetail.tsx` (thumbnail + main gallery)
**Description:** Images likely rendered with default loading; thumbs below viewport not explicitly `loading="lazy"`.
**Impact:** Wasted bandwidth on page load for images the user may never view.

---
## BUG-132: Related products grid renders full ProductTile including animations
**Severity:** Low
**Category:** Performance

**Location:** `src/app/shop/[id]/ProductDetail.tsx:316-348`
**Description:** Each related tile mounts its own hover animation hooks (`useReducedMotion`, motion components) even when off-screen.
**Impact:** CPU/memory for below-fold tiles that may never be seen.

---
## BUG-133: Cart/Wishlist `ids` iterated via `.find(products, id)` per item — O(n*m)
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/RecentlyViewedStrip.tsx:19-23`, `src/app/wishlist/page.tsx`
**Description:** Nested `.map -> .find` scales as items × catalog. For large catalogs this is quadratic.
**Impact:** Render time grows faster than necessary.
**Evidence:**
```tsx
ids.map((id) => products.find((p) => p.id === id))
```

---
## BUG-134: Every `useCurrency` consumer re-renders on unrelated updates
**Severity:** Medium
**Category:** Performance

**Location:** `src/lib/currency.tsx` (provider value)
**Description:** Without `useMemo` around the context value, every parent re-render re-renders every descendant that reads currency — and nearly every product row reads it via `format()`.
**Impact:** Cascade re-renders across entire product grids on every tick.

---
## BUG-135: `AnimatePresence` in `CartDrawer` keeps drawer DOM mounted
**Severity:** Low
**Category:** Performance

**Location:** `src/components/CartDrawer.tsx`
**Description:** Even when closed, exit animation leaves DOM mounted briefly. Repeated open/close churns the image pipeline for thumbnails.
**Impact:** Minor thumbnail re-decode cost on frequent open/close.

---
## BUG-136: `JSON.stringify(items)` on every cart change
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/cart.tsx:65`
**Description:** Full items array serialized and written to localStorage on every state change, including quantity bumps.
**Impact:** Tiny for small carts; scales linearly with cart size.

---
## BUG-137: `document.body.style.overflow = 'hidden'` causes layout recompute
**Severity:** Low
**Category:** Performance

**Location:** `src/components/CartDrawer.tsx:22`
**Description:** Directly mutating body style triggers a reflow; better to toggle a class.
**Impact:** Scrollbar reflow on drawer open.

---
## BUG-138: No code-splitting on CartDrawer — ships on every page
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/layout.tsx` (root layout renders CartDrawer)
**Description:** Cart drawer (and its framer-motion deps) load on every route, including the landing page where cart isn't surfaced.
**Impact:** Unnecessary JS on first-paint for pages that never show the cart.

---
## BUG-139: Font loader pulls multiple weights / subsets
**Severity:** Low
**Category:** Performance

**Location:** `src/app/layout.tsx` (Inter + JetBrains Mono)
**Description:** Unless explicitly restricted, `next/font/google` pulls wide weight ranges.
**Impact:** Extra font bytes; CLS risk.

---
## BUG-140: Large inline SVG in `Logo` re-shipped on every page (no <link rel=preload> / no sprite)
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Logo.tsx`
**Description:** Inlined SVG is duplicated in every navigation payload instead of cached as a static asset.
**Impact:** Repeated HTML payload bloat.

---
## BUG-141: FormField does not set `aria-invalid` / `aria-describedby` on error
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/FormField.tsx:42-51`
**Description:** Error text appears but the input has no `aria-invalid="true"` and no link to the error via `aria-describedby`.
**Impact:** Screen readers don't announce the invalid state or link the error to the field.

---
## BUG-142: Checkout FormField instances don't forward `required` / `aria-required`
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:223-309`
**Description:** No required/aria-required passed to FormField for email/name/address.
**Impact:** Browser validation + AT announcements missing.

---
## BUG-143: Checkout order summary `lg:sticky lg:top-6` collides with form on mid-viewport
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:379`
**Description:** Sticky summary aside can overlap/duplicate form fields when scrolled on narrow `lg` widths with short viewport heights.
**Impact:** Field occlusion; tab-order confusion.

---
## BUG-144: `.prose-underground a` links indistinguishable from body text
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/globals.css:86-90`
**Description:** Only an underline differentiates links; no color shift. Fails WCAG 1.4.1 (use of color) when underline is rendered thin on mobile.
**Impact:** Low-vision users miss links in legal copy.

---
## BUG-145: FormField error uses `text-red-700` without contrast verification
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/FormField.tsx:49`
**Description:** Paired with a parchment `#F5F4F0` background the small uppercase mono text may not meet 4.5:1.
**Impact:** WCAG 1.4.3 contrast risk on error copy.

---
## BUG-146: Newsletter error message has no `aria-live`/`role="alert"` region
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Newsletter.tsx:80-84`
**Description:** "Invalid email" appears visually but is not announced.
**Impact:** AT users don't hear validation feedback.

---
## BUG-147: Hamburger toggle is 40×40 — below 44×44 WCAG target size
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Nav.tsx:122`
**Description:** `w-10 h-10` gives a 40px hit area on mobile.
**Impact:** WCAG 2.5.5 target-size violation.
**Evidence:**
```tsx
className="md:hidden ... w-10 h-10 -mr-2 ..."
```

---
## BUG-148: CartDrawer qty +/- buttons are 28×28 — well below 44px
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/CartDrawer.tsx:141-152`
**Description:** `w-7 h-7` touch targets for quantity stepper.
**Impact:** Mistaps on mobile.

---
## BUG-149: LegalPage heading hierarchy mismatched visually
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/LegalPage.tsx:21`, `src/app/globals.css:56-67`
**Description:** Page `<h1>` is large; nested `<h2>` is styled at 0.7rem — visually less important than the body text.
**Impact:** Visual hierarchy misleads sighted scanning; AT users see `h2` nodes that read less prominently than surrounding text.

---
## BUG-150: LegalPage missing `main` landmark semantics / skip link
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/LegalPage.tsx:14-30`
**Description:** No explicit `role="main"` (only implicit `<main>`) and no skip link from Nav to main.
**Impact:** WCAG 2.4.1 bypass-blocks failure.

---
## BUG-151: Root layout does not export Next.js `viewport` config
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/layout.tsx`
**Description:** Next 15 wants `export const viewport: Viewport = {...}`. Without it, the viewport meta is default and cannot be tuned (initial-scale, themeColor, colorScheme).
**Impact:** No theme-color for mobile chrome; defaults may not match dark/light design.

---
## BUG-152: Custom cursor uses `z-[9999]` creating a brittle z-index ceiling
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Cursor.tsx:55`
**Description:** Future modals/drawers have no layer above the cursor; `mix-blend-difference` may also invert over unintended surfaces.
**Impact:** Stacking collisions when new overlays are added.

---
## BUG-153: `PageTransition` wrapper has no `role="region"` / `aria-live="polite"`
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/PageTransition.tsx:13-21`
**Description:** Route changes are not announced.
**Impact:** AT users don't get route-change feedback.

---
## BUG-154: Checkout shipping-method selector uses `aria-pressed` buttons, not a radio group
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:316-343`
**Description:** Mutually-exclusive choices should be `role="radiogroup"` with `role="radio"`.
**Impact:** Screen readers don't recognize the single-select semantics.

---
## BUG-155: Checkout submit button does not set `aria-busy` while pending
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx:363-369`
**Description:** Visual "Placing order…" copy changes, but no `aria-busy="true"` / live region.
**Impact:** ATs don't announce in-flight submit.

---
## BUG-156: `Logo` has no `<img>` fallback if SVG inlining fails at runtime
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Logo.tsx:23-33`
**Description:** If `innerSvg` is empty (e.g., malformed SVG), renders empty SVG with just `aria-label`.
**Impact:** Brand mark invisible; no graceful `<img src="/logo.svg">` fallback.

---
## BUG-157: Footer `text-black/50` on small 12px text may fail AA contrast
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Footer.tsx:41-42, 55`
**Description:** 50% opacity on light parchment likely fails 4.5:1 at small text sizes.
**Impact:** WCAG 1.4.3 contrast failure on footer links.

---
## BUG-158: Lookbook captions rely on `mix-blend-difference` with no readability fallback
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/lookbook/page.tsx:68-71`
**Description:** Blend-mode text can become illegible over mid-tone image regions.
**Impact:** Caption unreadable on some photos.

---
## BUG-159: Landing `/` route has no visible `<h1>` (FR-L8 strips header)
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/page.tsx`
**Description:** Document heading outline starts from `<h2>`/none — hurts SEO and AT nav.
**Impact:** Outline/accessibility scanning degraded.

---
## BUG-160: External links in Footer/SocialRow likely missing `rel="noopener noreferrer"`
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/SocialRow.tsx`, `src/components/Footer.tsx`
**Description:** `target="_blank"` without `rel="noopener noreferrer"` opens a tab-nabbing vector and leaks `window.opener`.
**Impact:** Security + privacy risk on external links.

---
## BUG-161: Shipping fee hardcoded — no country/city rate logic
**Severity:** High
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:104`
**Description:** `shipFee = subtotal >= 3000 ? 0 : shipMethod === "express" ? 160 : 80;` — same rate regardless of destination city/country.
**Impact:** International orders priced same as local; under-charging or over-charging depending on geography.

---
## BUG-162: Displayed "total" omits tax
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:105`, `src/app/cart/page.tsx:158`
**Description:** `total = subtotal + shipFee;` — no VAT line, no inclusive/exclusive indicator, even though a "Taxes: At checkout" string is shown.
**Impact:** User is promised one total, pays another at real checkout.

---
## BUG-163: Average-rating logic duplicated between `ShopGrid` and `lib/products`
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx:29-31`, `src/lib/products.ts:44-46`
**Description:** Two implementations of the same computation — risks drift over time.
**Impact:** Maintenance hazard.

---
## BUG-164: Related products don't filter out sold-out
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:46`
**Description:** `products.filter((p) => p.id !== product.id).slice(0, 4)` — no stock guard, no category preference.
**Impact:** Sold-out or unrelated products surface as "more pieces".

---
## BUG-165: Empty-shop "nothing matches" message regardless of cause
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx:208`
**Description:** When filters + "in stock only" zero out the list, the copy blames the query instead of explaining that everything's sold out.
**Impact:** Misleading UX.

---
## BUG-166: Pre-order form allows sizes the selected product doesn't offer
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/pre-order/page.tsx:10`
**Description:** Hardcoded `SIZES = ["XS", "S", "M", "L", "XL", "XXL"]`. Signature product only sells M/L/XL — nothing gates the UI.
**Impact:** User can pre-order a size that will never ship.
**Evidence:**
```tsx
const SIZES = ["XS", "S", "M", "L", "XL", "XXL"] as const;
```

---
## BUG-167: Pre-order doesn't validate product id against catalog
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/pre-order/page.tsx:26`
**Description:** Validation only checks `!values.product` (non-empty string) — a stale/removed id still passes.
**Impact:** Orphan submissions.

---
## BUG-168: Contact form 10-char minimum with no live counter
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/contact/page.tsx:24`
**Description:** Validator requires `message.length >= 10` but UI has no counter or progress hint.
**Impact:** Users hit the error without understanding the rule.

---
## BUG-169: Pre-order form doesn't block submit on sold-out / waitlist-only products
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/pre-order/page.tsx` (validation/step flow)
**Description:** Submission logic doesn't check product's `soldOut` state before accepting pre-order.
**Impact:** Collects demand for stock that may never ship.

---
## BUG-170: lookbook JSON cast without runtime validation
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/lookbook.ts:18`
**Description:** `export const lookbook: Lookbook = raw as Lookbook;` — trust-cast, no shape check.
**Impact:** Malformed JSON crashes the lookbook route with no error boundary.

---
## BUG-171: Mock order `stageIdx` always deterministic — same seed → same stage forever
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/orders/page.tsx:36`
**Description:** `const stageIdx = seed % STAGES.length;` freezes status at one value; a real order "progresses", this one never advances.
**Impact:** "Tracking" page shows the same stage indefinitely.

---
## BUG-172: Currency conversion done once on subtotal — breaks line-item reconciliation in USD/EUR
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/currency.tsx:67-73`
**Description:** `egp * RATES[cur]` applied to the aggregated subtotal. Line items shown elsewhere (cart rows, wishlist) format each price individually. Sum of formatted line items can differ from formatted subtotal by rounding pennies.
**Impact:** Visible "Σ line items ≠ subtotal" in cart.

---
## BUG-173: Wishlist add-to-cart ignores product `soldOut` if guard is removed/changed
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/wishlist/page.tsx:62, 96-104`
**Description:** Add button is gated by `!p.soldOut` today, but the `defaultSize` logic runs regardless and assumes `p.sizes` is populated — fragile coupling.
**Impact:** Future refactors that remove the guard silently add sold-out products to cart.

---
## BUG-174: ProductDetail fallback sizes override product's single "One size" variant
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/shop/[id]/ProductDetail.tsx:25`
**Description:** `product.sizes ?? ["S","M","L","XL"]` — if any product models "one size" as `sizes: ["One size"]` it's fine, but products with an empty `sizes: []` fall through to defaults.
**Impact:** Wrong size set shown for accessories / one-size items when data uses `[]`.

---
## BUG-175: `recentlyViewed` dedupe relies on `filter` before `unshift` — corrupted storage can double entries
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/recentlyViewed.tsx` (recordView)
**Description:** `current.filter(x => x !== id); current.unshift(id);` — fine for normal writes, but a pre-corrupted list containing duplicates is never deduped across the whole array.
**Impact:** Strip can show duplicates.

---
## BUG-176: Shop price filter (if any) likely uses `<=` on both ends — no handling of open ranges
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx` (filter logic)
**Description:** Under-priced edge (min filter = 0 EGP) may be omitted depending on operator; open-ended bands not supported.
**Impact:** Unexpectedly excluded products.

---
## BUG-177: BUG-058 correction — cart IS keyed by `${id}__${size}`; concern was wrong
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:75, 77`
**Description:** BUG-058 logged that id+size rows would collapse. Re-reading the file shows `lineId = ${product.id}__${size}` with existing lookup by `lineId`. The collapse bug does NOT exist.
**Impact:** N/A — correcting prior false-positive for the record.
**Evidence:**
```tsx
const lineId = `${product.id}__${size}`;
const existing = prev.find((i) => i.id === lineId);
```

---
## BUG-178: "Free shipping unlocked" comparison uses `===` on a `Math.max`-produced number
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/cart/page.tsx` (FREE_SHIP_THRESHOLD messaging)
**Description:** `remaining === 0` works for integer EGP, but after any currency switch the remaining may be a float (e.g., 0.0000001) after conversion, breaking the "unlocked" branch.
**Impact:** In non-EGP currency, threshold banner flickers near the boundary.

---
## BUG-179: `recentlyViewed` MAX cap enforced via `.slice(0, MAX)` — no LRU on identical entries
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/recentlyViewed.tsx` (save())
**Description:** Re-viewing the 9th item pushes a fresh entry that bumps #8 off — acceptable semantics, but `MAX` is hardcoded with no config / config-file reference.
**Impact:** Tunability friction.

---
## BUG-180: Checkout email never trimmed before validation
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx` (validate())
**Description:** Leading/trailing spaces from paste pass or fail validation unpredictably depending on regex.
**Impact:** False negatives for pasted emails.

---
## BUG-181: `/shop` has no `revalidate` or `dynamic` export — catalog frozen at build
**Severity:** High
**Category:** API & Data

**Location:** `src/app/shop/page.tsx`
**Description:** Page imports the `products` module and is statically generated; no ISR / dynamic mode. Any products.json change requires a rebuild.
**Impact:** Stale catalog; new drops invisible until CI redeploys.

---
## BUG-182: `/shop/[id]` `generateStaticParams` freezes the set of routable products at build
**Severity:** High
**Category:** API & Data

**Location:** `src/app/shop/[id]/page.tsx:7-9`
**Description:** Only product ids present at build are emitted. Deleted products still resolve from old HTML; new ids 404 until rebuild.
**Impact:** Dead product pages live on; new drops unreachable.

---
## BUG-183: Cart not synced across tabs (no `storage` event listener)
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx`
**Description:** No `window.addEventListener("storage", ...)` to pick up writes from other tabs.
**Impact:** Second tab shows stale cart; checkout can proceed with wrong totals.

---
## BUG-184: Wishlist not synced across tabs
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/wishlist.tsx`
**Description:** No storage-event sync.
**Impact:** Hearts stale across tabs.

---
## BUG-185: Currency preference not synced across tabs
**Severity:** Low
**Category:** API & Data

**Location:** `src/lib/currency.tsx`
**Description:** No storage-event sync.
**Impact:** Mixed currencies across tabs.

---
## BUG-186: Rapid wishlist toggle writes localStorage on every change (no coalescing)
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/wishlist.tsx:64-66` (persistence effect)
**Description:** Every toggle serializes the whole array synchronously; no debounce.
**Impact:** Burst writes can exceed quota silently.

---
## BUG-187: Cart quantity bumps write localStorage per click
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx:62-69`
**Description:** Same pattern — each `+`/`−` fires a full serialization.
**Impact:** Storage thrash; accelerates quota pressure.

---
## BUG-188: No optimistic-UI rollback when localStorage write fails
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx`, `src/lib/wishlist.tsx`
**Description:** Silent catch swallows write failures; in-memory state is kept but not persisted. User sees the item added; reload drops it.
**Impact:** Phantom-persistence bug with no feedback.

---
## BUG-189: localStorage keys have `.v1` version but no migration logic
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/cart.tsx:38`, `src/lib/wishlist.tsx:13`, `src/lib/currency.tsx:27`, `src/lib/recentlyViewed.tsx:5`
**Description:** Keys imply versioning but nothing reads/writes a version field or migrates schema.
**Impact:** First breaking schema change silently invalidates every user's saved state.

---
## BUG-190: No `public/robots.txt` or `app/robots.ts`
**Severity:** Medium
**Category:** API & Data

**Location:** project root
**Description:** No crawl directives; checkout / cart / orders / wishlist pages are all indexable.
**Impact:** SEO noise; private flows can appear in Google results.

---
## BUG-191: No `public/sitemap.xml` or `app/sitemap.ts`
**Severity:** Medium
**Category:** API & Data

**Location:** project root
**Description:** Missing sitemap entirely.
**Impact:** Search engines must discover product pages unaided.

---
## BUG-192: Only root + product detail export metadata — 8+ routes rely on root defaults
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/cart/page.tsx`, `src/app/checkout/page.tsx`, `src/app/contact/page.tsx`, `src/app/pre-order/page.tsx`, `src/app/wishlist/page.tsx`, `src/app/orders/page.tsx`, `src/app/home/page.tsx`, `src/app/lookbook/page.tsx`
**Description:** No per-page metadata/OG image.
**Impact:** Weak social previews; identical titles in search results.

---
## BUG-193: Hero image hardcoded to Unsplash with query params that may lapse
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/home/page.tsx:16-17, 66-73`
**Description:** No `onError` fallback; URL is a deep-linked asset that Unsplash can revoke.
**Impact:** Brand page with a broken hero.

---
## BUG-194: All product images in `products.json` point at Unsplash
**Severity:** Medium
**Category:** API & Data

**Location:** `src/content/products.json`
**Description:** Every `image` field is an external Unsplash URL. `next.config` must allow-list remote patterns, and there is no CDN fallback.
**Impact:** Single-point failure for the whole catalog if Unsplash blocks/removes content.

---
## BUG-195: Mock form handlers with `TODO(template-buyer)` comments ship to production
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/contact/page.tsx:38-48`, `src/app/checkout/page.tsx:107-128`, `src/app/pre-order/page.tsx:47-57`
**Description:** Await a `setTimeout` and then pretend success. If shipped, users "submit" contact/checkout/pre-order and nothing happens server-side.
**Impact:** Silent data loss for any real user interaction.

---
## BUG-196: No CSP / `Content-Security-Policy` configured
**Severity:** Medium
**Category:** API & Data

**Location:** `next.config` / `middleware.ts`
**Description:** No CSP header set. App uses `dangerouslySetInnerHTML` in Logo, so a future supply-chain incident in logo.svg would run inline scripts.
**Impact:** XSS surface area wider than necessary.

---
## [FALSE_POSITIVE] BUG-197: Next `images.remotePatterns` must whitelist `images.unsplash.com`

> **Resolution:** Not a bug — `next.config.ts` already contains `remotePatterns: [{ protocol: "https", hostname: "images.unsplash.com" }]`. No fix needed.

**Severity:** Medium
**Category:** API & Data

**Location:** `next.config.*`
**Description:** If `next.config` is missing an entry for Unsplash, `<Image>` renders will fail at runtime.
**Impact:** Build passes, runtime explodes when a product page is opened.

---
## [SKIPPED] BUG-198: No service worker / offline story

> **Resolution:** Skipped — adding PWA/service worker is a feature, not a bug fix. Out of scope for minimalist marketing site per PRD; requires product decision, new dependencies, manifest, and caching strategy.

**Severity:** Low
**Category:** API & Data

**Location:** project-wide
**Description:** Zero PWA / offline behaviour; a flaky connection produces blank pages.
**Impact:** Mobile users with spotty connectivity lose context.

---
## [FIXED] BUG-199: No fetch-priority / resource hints

> **Resolution:** Added `<link rel="preconnect" href="https://images.unsplash.com" crossOrigin="anonymous" />` to the root layout head so the browser opens a TCP/TLS connection to Unsplash before the first `<Image>` request, improving LCP.

**Severity:** Low
**Category:** API & Data

**Location:** `src/app/layout.tsx`
**Description:** No `<link rel="preconnect">` to images.unsplash.com, no `fetchPriority="high"` on hero Image beyond `priority`.
**Impact:** Slower LCP.

---
## [FIXED] BUG-200: `useRecentlyViewed`'s custom-event bus is in-process only — no cross-tab echo

> **Resolution:** Added a `storage` event listener in `useRecentlyViewed` that re-reads the list when the recently-viewed localStorage key changes in another tab, providing cross-tab sync while leaving the same-tab custom-event path intact.

**Severity:** Low
**Category:** API & Data

**Location:** `src/lib/recentlyViewed.tsx`
**Description:** Custom "underground:recently-viewed-changed" event fires only inside the current window. Tab B doesn't update.
**Impact:** Recently-viewed diverges across tabs.

---
## [FIXED] BUG-201: ShopGrid "featured" sort is a no-op (default case returns 0)

> **Resolution:** Replaced the `default: return 0` with a deterministic comparator that surfaces signature pieces first, then in-stock items, relying on V8's stable sort for ties. "Featured" now reflects a real curation rather than input order.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:71-72`
**Description:** `case default: return 0` means featured sort leaves items in incoming order — but `.sort()` is applied to a copy of the filtered list whose order is itself dependent on the source array. The "featured" label doesn't reflect a deterministic curation.
**Impact:** "Featured" is actually "whatever order data is in."
**Evidence:**
```tsx
default: return 0;
```

---
## [SKIPPED] BUG-202: ShopGrid filter + sort state isn't reflected in URL (no useSearchParams)

> **Resolution:** Skipped — URL state sync is a feature enhancement, not a surgical fix. Proper implementation requires `useSearchParams`/`useRouter`, a Suspense boundary (Next 15), param parsing/validation, and history management. Flag for a dedicated ticket.

**Severity:** Medium
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:45-48`
**Description:** `filter`, `sort`, `query`, `hideSoldOut` are local component state only. Navigating away and back resets; sharing URLs doesn't preserve state.
**Impact:** Back-button regression; no shareable filter links.

---
## [FIXED] BUG-203: ShopGrid sort spreads `filtered` then sorts — new array each memo call

> **Resolution:** Dropped the redundant `[...list]` spread. `regular.filter(...)` already produces a fresh array, and `.sort()` mutates it in place, so the extra clone was wasted work.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:61`
**Description:** `list = [...list].sort(...)` — the intermediate `.filter` array is already fresh; the extra spread is wasted.
**Impact:** Negligible — but creates a subtle cue for downstream identity-based memoization.

---
## [FIXED] BUG-204: `ts()` calls `new Date(...)` on every sort compare

> **Resolution:** When `sort === "newest"`, build a `Map<id, ts>` once before `.sort()` and look up cached timestamps in the comparator. Avoids re-parsing `releasedAt` on every O(n log n) comparison.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:34-36`
**Description:** Sort comparator parses the same date repeatedly.
**Impact:** Wasteful in large catalogs.

---
## [FIXED] BUG-205: `avg()` recomputed on every sort compare for rating

> **Resolution:** When `sort === "rating"`, build a `Map<id, avg>` once before `.sort()` and look up cached averages in the comparator. Avoids recomputing the reduce-over-reviews on every O(n log n) comparison.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:29-31, 70`
**Description:** Same — comparator computes averages for every pair comparison (O(n·n·k)).
**Impact:** Quadratic-ish cost on rating sort.

---
## [FIXED] BUG-206: ShopGrid `filtered` memo retains stale `filter` label after catalog removes category

> **Resolution:** Added a `useEffect` that reconciles `filter` against `regular` — if the selected category has zero matching products, it resets to `"all"` so the UI doesn't strand the user on an empty view with a lingering category pill.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:45`
**Description:** `filter` state isn't reconciled against available categories. If `regular` no longer contains any products of a chosen category, UI persists the filter and shows empty state.
**Impact:** Confusing empty view with lingering category pill active.

---
## [FIXED] BUG-207: `hideSoldOut` state is independent of the `soldOut` distribution

> **Resolution:** Added a `hasSoldOut` memo; the "In stock only" toggle only renders when at least one product has `soldOut`, and a reconciling `useEffect` clears the state if the distribution later changes.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:48`
**Description:** If catalog has no sold-out items, toggle is still visible. Should disable or hide until at least one soldOut exists.
**Impact:** UI cruft.

---
## [FIXED] BUG-208: `showSignature` derived inline without memo

> **Resolution:** Wrapped the `showSignature` derivation in `useMemo` keyed on `[signatures, filter, query]` so it's consistent with the surrounding `filtered` memo.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:78-79`
**Description:** Boolean derivation each render.
**Impact:** Negligible, but pattern is inconsistent with `filtered` (which is memoized).

---
## [FIXED] BUG-209: ShopGrid `query` input is fully controlled but no setter debounce

> **Resolution:** Consumed `query` through React 19's `useDeferredValue` inside the `filtered` memo, so the input updates immediately while the filter/sort work is deferred to a lower-priority render. Input display and active chips still use the immediate value.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx:47`
**Description:** Every keystroke triggers the whole memo. No `useDeferredValue` / debounce.
**Impact:** Perceptible input lag as catalog grows.

---
## [FALSE_POSITIVE] BUG-210: Nav mobile-menu state not closed on route change

> **Resolution:** Not a bug — `Nav.tsx:32-34` already has `useEffect(() => { setOpen(false); }, [pathname])`, which closes the mobile menu whenever the pathname changes.

**Severity:** Medium
**Category:** State Management

**Location:** `src/components/Nav.tsx`
**Description:** If the mobile menu state is not reset when `pathname` changes, tapping a nav link leaves the menu visually open after navigating.
**Impact:** Mobile users see stale menu on new page.

---
## [FIXED] BUG-211: `CartDrawer` `isOpen` is not closed automatically on route change

> **Resolution:** Added a `usePathname`-keyed `useEffect` in `CartDrawer` that calls `closeCart()` whenever the route changes. Defensive against future internal links that might navigate without manually closing the drawer first.

**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/cart.tsx` / `src/components/CartDrawer.tsx`
**Description:** If the user clicks a product link from inside the drawer, the drawer persists across the navigation.
**Impact:** Obscures the new page until user manually closes.

---
## [FIXED] BUG-212: `checkout` `done` state persists across cart refill

> **Resolution:** Added a `useEffect` that clears the `done` state whenever `items.length > 0`, so refilling the cart after a successful order dismisses the stale confirmation screen. The submit handler batches `setDone` + `clear`, so the confirmation still shows immediately after ordering.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/checkout/page.tsx`
**Description:** After order placement, `done` is set; if the user adds new items and returns to /checkout, the "order placed" screen may still show until component fully remounts.
**Impact:** Confusing stale confirmation screen.

---
## [FIXED] BUG-213: `orders` page `order` state isn't cleared between lookups

> **Resolution:** Added `setOrder(null)` at the top of `handleSubmit` alongside the existing `setError(null)` so any previous result is cleared before a new lookup completes. Defensive — current UI only shows the form when `order` is null, but this prevents stale state if an in-place re-lookup is wired in later.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/orders/page.tsx:57-70`
**Description:** If second lookup fails, the first successful result may still render.
**Impact:** Wrong order shown next to error message.

---
## [FALSE_POSITIVE] BUG-214: `Reviews` page-local filter state isn't persisted per product

> **Resolution:** Not a bug — resetting the star-rating filter when navigating between products is correct UX. Each product's reviews are independent, and silently carrying "3-star only" from Product A onto Product B would surprise users, not help them.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/Reviews.tsx`
**Description:** Switching between product pages resets filter each time.
**Impact:** Mildly annoying UX.

---
## [FALSE_POSITIVE] BUG-215: `ProductDetail` `activeIdx` doesn't reset when `product.id` changes

> **Resolution:** Not a bug — `ProductDetail.tsx:44-51` already has a `useEffect` keyed on `product.id` that resets `activeIdx`, `size`, `qty`, and other per-product state. Additionally, `safeIdx = Math.min(activeIdx, gallery.length - 1)` on line 28 defensively clamps the rendered image to a valid bound.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx`
**Description:** Navigating between related products reuses the same component instance; `activeIdx=3` on previous product leaks into new product's gallery (may exceed bounds).
**Impact:** Gallery opens on wrong / missing image for the new product.

---
## [FALSE_POSITIVE] BUG-216: `ProductDetail` `selectedSize` state doesn't validate against new product

> **Resolution:** Not a bug — the same `useEffect([product.id])` at `ProductDetail.tsx:44-51` calls `setSize(null)` on product change, so a previous product's size selection cannot leak. `handleAdd` then short-circuits on `!size` until the user picks a valid option from the new product's `sizes` array.

**Severity:** Medium
**Category:** State Management

**Location:** `src/app/shop/[id]/ProductDetail.tsx`
**Description:** Same cross-product state reuse: previously selected "XS" sticks when navigating to a product that only sells M/L/XL.
**Impact:** Can try to add-to-cart with a size the new product doesn't offer.

---
## [FALSE_POSITIVE] BUG-217: `Nav` active-link state derived from pathname — no memoization

> **Resolution:** Not a bug — `Nav.tsx` does not compute an active-link className from `pathname`. Links use static `hover:opacity-70` classes only; `pathname` is consumed solely by the mobile-menu close effect. Nothing to memoize.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/Nav.tsx`
**Description:** Active-class computation runs each render.
**Impact:** Negligible.

---
## [FALSE_POSITIVE] BUG-218: `Cursor` `x,y` refs mutated inside RAF — never deref'd in effect cleanup

> **Resolution:** Not a bug — `pos`, `target`, `scale`, and `targetScale` in `Cursor.tsx` are effect-local closure variables, not refs. When cleanup cancels the RAF and removes the listeners, the loop closure becomes unreachable and is garbage-collected. Only `dotRef` is a ref, and it's bound to the DOM element, not mutation state.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/Cursor.tsx`
**Description:** Cleanup cancels RAF but leaves mutation-bearing refs pinned; minor GC pressure.
**Impact:** Minimal.

---
## [FALSE_POSITIVE] BUG-219: Pre-order `form` state persists after success screen renders

> **Resolution:** Not a bug — the success screen has no dismiss path; the only exit is a `BarButton` to `/shop`, which unmounts the page. Returning to `/pre-order` remounts with empty state. Also, `form.email` is intentionally retained so the confirmation message can render the user's email.

**Severity:** Low
**Category:** State Management

**Location:** `src/app/pre-order/page.tsx`
**Description:** Submitting doesn't reset `form` — if the success view is dismissed (if ever), the old values preload the form.
**Impact:** Stale values on re-submit flow.

---
## [FALSE_POSITIVE] BUG-220: Newsletter `email` cleared on success but error toast keeps reading from stale `email`

> **Resolution:** Not a bug — the error toast reads from a dedicated `errorMsg` state, not `email`, so clearing `email` on success can't produce a stale error read. Also, when `state === "ok"` the form is replaced by the success message, so there is no re-subscribe surface in the same mount.

**Severity:** Low
**Category:** State Management

**Location:** `src/components/Newsletter.tsx`
**Description:** After "ok" state, email is reset; rapid resubscribe typing may mix `state` transitions with the reset value window.
**Impact:** Flicker between "ok" and "busy" visuals.

---
## [SKIPPED] BUG-221: No Sentry / telemetry / logging SDK installed anywhere

> **Resolution:** Skipped — integrating telemetry requires a new production dependency, an account/DSN, and Next.js client+server+edge wiring. That's the template buyer's responsibility, matching the other `TODO(template-buyer)` integration points (payments, waitlist). Flagged for post-sale setup.

**Severity:** High
**Category:** Error Handling

**Location:** project-wide (`package.json` has no error-tracking dep)
**Description:** Zero error observability. Swallowed catches + uncaught rejections never reach the team.
**Impact:** Production errors are invisible until users complain.

---
## [FALSE_POSITIVE] BUG-222: `Logo.tsx` `readFileSync` throws without `process.env.NODE_ENV` guard for dev vs prod

> **Resolution:** Not a bug — `Logo.tsx:10-20` wraps `readFileSync` in a `try/catch` that logs a warning and falls back to `FALLBACK_INNER` / `FALLBACK_VIEWBOX` (a text wordmark). `ENOENT` degrades gracefully instead of crashing the route.

**Severity:** Medium
**Category:** Error Handling

**Location:** `src/components/Logo.tsx`
**Description:** If the build is bundled for serverless and the `public/` directory isn't copied relative to the output, `readFileSync` fails at runtime with a cryptic `ENOENT`.
**Impact:** Every page on every route returns 500.

---
## [FALSE_POSITIVE] BUG-223: `lib/products.ts` `findProduct` returns undefined silently

> **Resolution:** Not a bug — both call sites in `src/app/shop/[id]/page.tsx` guard the undefined return (metadata returns "Not found", page calls `notFound()`). The `Product | undefined` return type is idiomatic TypeScript and forces compile-time handling at any future caller.

**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/products.ts`
**Description:** Return type is `Product | undefined` with no breadcrumb; callers that forget the guard (and there are some) crash deep in the tree.
**Impact:** Hidden coupling to nullability.

---
## [FALSE_POSITIVE] BUG-224: `Stars.tsx` doesn't guard for negative or >5 ratings

> **Resolution:** Not a bug — `Stars.tsx:16` already does exactly the clamp the bug description suggests: `fill = Math.max(0, Math.min(1, value - (i - 1)))`. Out-of-range values produce all-empty or all-full stars without breaking layout.

**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/Stars.tsx`
**Description:** Math like `fill = Math.min(1, Math.max(0, rating - i))` is common; if absent, a rogue 6.3 rating breaks layout.
**Impact:** Malformed data crashes the star row.

---
## [FIXED] BUG-225: CartDrawer image errors inside the drawer have no fallback

> **Resolution:** Extracted a small `LineItemImage` helper that listens for `onError` and swaps the src to `/fallback-garment.svg` (the project's conventional garment fallback). Includes a self-check to avoid infinite loops if the fallback itself 404s.

**Severity:** Medium
**Category:** Error Handling

**Location:** `src/components/CartDrawer.tsx` (line-item `<Image>`)
**Description:** Cart line items render the raw stored image URL. If the URL 404s, drawer shows a broken-image icon with no swap.
**Impact:** Broken visuals during the highest-intent moment.

---
## [FIXED] BUG-226: `window.localStorage.getItem` throws on first access in certain Safari private modes

> **Resolution:** Converted the silent `catch {}` blocks in `cart.tsx`, `wishlist.tsx`, `currency.tsx`, and `recentlyViewed.tsx` to `catch (err) { console.warn(...) }` with context-prefixed messages. Safari-private-mode quota failures and corrupted-JSON errors are now observable in devtools without altering the fallback behavior.

**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:43`, `src/lib/wishlist.tsx:29`, `src/lib/currency.tsx:43`, `src/lib/recentlyViewed.tsx:10`
**Description:** Wrapped in try/catch, but catches are empty — no telemetry hook to distinguish "first-use failure" from "corrupted JSON".
**Impact:** Can't diagnose private-mode issues from logs.

---
## [FALSE_POSITIVE] BUG-227: `useCart` / `useWishlist` / `useCurrency` hooks throw if provider is missing

> **Resolution:** Not a bug — `src/app/error.tsx` is a Next.js App Router error boundary that catches render-time throws across the app, including missing-provider errors. The user sees the branded "the thread snapped" screen with "Try again" and "Back to shop" recovery, not a white screen. The boundary also logs the error to the console for devs.

**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:121`, others
**Description:** `throw new Error("useCart must be used inside <CartProvider>")` — a descriptive throw, but there's no error boundary to catch it.
**Impact:** Missing provider = whole-app white screen.
**Evidence:**
```tsx
if (!ctx) throw new Error("useCart must be used inside <CartProvider>");
```

---
## [FALSE_POSITIVE] BUG-228: `generateMetadata` for product page has no try/catch

> **Resolution:** Not a bug — `generateMetadata` only calls non-throwing operations: `findProduct` is a pure `Array.find`, `Number.toLocaleString` is spec-safe, and the rest are plain template strings. Because products come from a static JSON imported at build time, any data-shape problem is caught at build time (fail fast), which is the intended behavior for a static site.

**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/shop/[id]/page.tsx:11-23`
**Description:** If `findProduct` / description access throws during metadata generation, the whole page build fails.
**Impact:** One bad product poisons the build.

---
## [FIXED] BUG-229: `JSON.parse` in stores throws `SyntaxError` — caught but not distinguished from real data errors

> **Resolution:** `cart.tsx`, `wishlist.tsx`, and `recentlyViewed.tsx` now branch on `err instanceof SyntaxError`: corrupted JSON logs a distinct "stored JSON is corrupted — purging" warning and calls `removeItem` on the bad key so the next load starts clean. Non-JSON errors (Safari-private-mode quota) log separately.

**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:42-49`, `src/lib/wishlist.tsx:29-35`, `src/lib/recentlyViewed.tsx:10-16`
**Description:** Bare `catch` treats malformed JSON the same as "no data at all" — user loses their cart silently.
**Impact:** Silent data loss, no recovery option.

---
## [FALSE_POSITIVE] BUG-230: No fallback for `matchMedia` (older browsers / SSR)

> **Resolution:** Not a bug — `window.matchMedia` is universally supported in the Next.js 15 / React 19 browser matrix (IE10/Safari 5.1+, 2012). Both call sites (`Cursor.tsx`, `home/page.tsx`) are inside `useEffect`, so `window` is guaranteed defined. `PageTransition` relies on framer-motion's `useReducedMotion`, which has its own internal guard.

**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/Cursor.tsx`, `src/app/home/page.tsx`, `src/components/PageTransition.tsx`
**Description:** `window.matchMedia` calls assume the API exists. Legacy browsers throw; cleanup path doesn't handle the case.
**Impact:** Edge-case JS crash.

---
## [FALSE_POSITIVE] BUG-231: `next/font` load failure has no fallback

> **Resolution:** Not a bug — both `Inter` and `JetBrains_Mono` in `layout.tsx:12-22` already declare `display: "swap"` (the bug description even admits it wasn't visible in the snippet the reporter saw). `next/font/google` downloads fonts at build time and self-hosts them; build-time CDN unreachability is a Next.js/CI infra concern, not addressable in application code.
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/layout.tsx`
**Description:** If Google Fonts is unreachable at build time, the build fails hard; `display: "swap"` not shown in this snippet.
**Impact:** Build brittleness.

---
## [FIXED] BUG-232: `new Date(p.releasedAt)` in `ShopGrid` `ts()` returns `NaN` on malformed strings — silent

> **Resolution:** Guarded `ts()` in `ShopGrid.tsx` with `Number.isNaN` — malformed `releasedAt` values now return 0 instead of NaN, so the "newest" sort is deterministic (bad dates sink to the end rather than sorting arbitrarily).
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/ShopGrid.tsx:34-36`
**Description:** NaN compares as false in `<`/`>` — malformed dates sort arbitrarily.
**Impact:** Unpredictable "newest" ordering if any product has a bad `releasedAt`.

---
## [FALSE_POSITIVE] BUG-233: `Reviews` date parsing has no guard

> **Resolution:** Not a bug — `Reviews.tsx` does not sort by date. `r.date` is only rendered as a raw string at line 107, and a repo-wide grep found no date-sort path for reviews. There's no NaN pathway to guard.
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/Reviews.tsx` (sort by date)
**Description:** Same pattern — bad dates yield NaN ordering.
**Impact:** Minor sort anomaly.

---
## [FALSE_POSITIVE] BUG-234: Framer Motion onAnimationStart/Complete errors have no handler

> **Resolution:** Not a bug — Framer Motion manages animation errors internally, and the app's top-level `src/app/error.tsx` boundary catches any error that does propagate through React. Wrapping every `motion.*` JSX element in try/catch isn't idiomatic, and no concrete throw site was cited.
**Severity:** Low
**Category:** Error Handling

**Location:** all files using `motion.*`
**Description:** If a transition throws (rare but possible with custom easings), the error propagates to React. No try/catch.
**Impact:** Edge crashes.

---
## [FALSE_POSITIVE] BUG-235: `checkout` generate-order-number could throw `RangeError` on overflow (if seed logic scaled)

> **Resolution:** Not a bug — the description explicitly states "current use is safe." This is speculative defensive coding for hypothetical future changes, which violates the project's "don't add validation for scenarios that can't happen" standard.
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx:29-32`
**Description:** Current use is safe, but future changes that multiply seed by larger factors without guard could overflow.
**Impact:** Latent bug.

---
## [FIXED] BUG-236: No `window.onerror` / `onunhandledrejection` handler exists to surface swallowed failures

> **Resolution:** Added `src/components/GlobalErrorListener.tsx` — a null-rendering client component that installs `window` `error` and `unhandledrejection` listeners and logs them with context. Mounted in `layout.tsx` inside `CartProvider` so it runs for every route.
**Severity:** Medium
**Category:** Error Handling

**Location:** project-wide
**Description:** No client-side global listener.
**Impact:** Silent production incidents.

---
## [FALSE_POSITIVE] BUG-237: `cart.add` assumes `product` is non-null — caller crash if a stale id is reached

> **Resolution:** Not a bug — `add(product: Product, ...)` is typed non-null and TypeScript strict rejects null/undefined at every call site. The product catalog is static (`products.json`), so no runtime ID lookup can return null before `add` runs. Adding a defensive null check would violate the project's "don't validate scenarios that can't happen" rule.
**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:74-97`
**Description:** `product.id`, `product.name`, etc. accessed directly — no runtime null guard.
**Impact:** Hard crash on programmatic misuse.

---
## [FALSE_POSITIVE] BUG-238: `Image` component uses `fill` without guaranteed positioned parent in several places

> **Resolution:** Not a bug — every `<Image fill>` usage in the codebase (checkout, cart, wishlist, tiles, hero, gallery, lookbook, recently-viewed) is wrapped in a `relative`-positioned parent. Next.js also throws a clear dev-time error if this invariant is ever broken, so a regression would be caught immediately.
**Severity:** Low
**Category:** Error Handling

**Location:** various tiles / cart drawer / hero
**Description:** If a parent's `position` is accidentally `static`, Next dev throws and prod silently misrenders.
**Impact:** Edge-case layout crash.

---
## [FALSE_POSITIVE] BUG-239: Orders "not found" path shows a generic message and clears prior order with no confirmation

> **Resolution:** Not a bug — the form is conditionally hidden (`{!order ? ... : ...}` at `orders/page.tsx:111`), so a failed re-lookup cannot overwrite a displayed order. The user must explicitly click "reset" to search again.
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/orders/page.tsx:57-70`
**Description:** A single failed lookup blows away the previously successful result.
**Impact:** Lost context on typos.

---
## [FIXED] BUG-240: Empty `catch {}` blocks total: 10+ across stores — none re-thrown or logged

> **Resolution:** Already resolved across earlier bug fixes — every primary catch in `cart.tsx`, `wishlist.tsx`, `recentlyViewed.tsx`, `currency.tsx`, `checkout/page.tsx`, `contact/page.tsx`, `pre-order/page.tsx`, `Newsletter.tsx`, and `Logo.tsx` now logs with a context prefix. The three remaining `catch {}` sites are nested cleanup (removing an already-corrupted localStorage key inside an outer catch that logged the SyntaxError) — intentional to avoid duplicate noise.
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/lib/cart.tsx`, `src/lib/wishlist.tsx`, `src/lib/currency.tsx`, `src/lib/recentlyViewed.tsx`, `src/app/checkout/page.tsx`
**Description:** Pattern-level smell: silent catches everywhere with no instrumentation.
**Impact:** Production signal fully suppressed.

---
## [FIXED] BUG-241: ShopGrid rating sort performs O(n²·k) work (avg recomputed per compare)

> **Resolution:** Already resolved in earlier bug basher work — `ShopGrid.tsx` builds an `avgCache` Map of per-product averages before sorting, and the comparator reads cached values. Work is now O(n·k + n log n) instead of O(n²·k).
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/ShopGrid.tsx:70`
**Description:** `avg(a)` / `avg(b)` called inside the comparator; each call reduces over all reviews.
**Impact:** Catalog + reviews growth yields quadratic time.

---
## [FIXED] BUG-242: ShopGrid `ts()` parses dates per comparison rather than decorate-sort-undecorate

> **Resolution:** Already resolved in earlier bug basher work — `ShopGrid.tsx` builds a `tsCache` Map of parsed timestamps before sorting, and the comparator reads cached values. Each date is parsed once, achieving decorate-sort-undecorate semantics.
**Severity:** Low
**Category:** Performance

**Location:** `src/components/ShopGrid.tsx:64`
**Description:** `new Date(p.releasedAt).getTime()` per compare.
**Impact:** Wasted parsing; mitigated by small n but still suboptimal.

---
## [FIXED] BUG-243: ProductTile uses `motion.div` + `useReducedMotion` per tile

> **Resolution:** Lifted `useReducedMotion()` from `ProductTile` up to its only consumer (`ShopGrid`), which calls the hook once and passes the boolean down as a `reduced` prop. Tile-level matchMedia subscriptions collapse from O(N tiles) to O(1).
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/ProductTile.tsx`
**Description:** Each grid tile mounts its own Framer hook; in a grid of 30 tiles, that's 30 motion instances subscribed to reduced-motion.
**Impact:** Memory + subscription count scale linearly.

---
## [FALSE_POSITIVE] BUG-244: `BarButton` ships Framer Motion even when reduced-motion is on

> **Resolution:** Not a bug — Framer Motion is a static ES import shared across `PageTransition`, `ShopGrid`, `Cursor`, `ProductTile`, etc., so it's already in the shared chunk regardless. Static imports can't branch on a runtime user preference, and dynamic splitting would just add a round-trip for non-reduced users.
**Severity:** Low
**Category:** Performance

**Location:** `src/components/BarButton.tsx`
**Description:** The reduced-motion branch still imports the full framer-motion module.
**Impact:** No bundle savings when animation is skipped.

---
## [FALSE_POSITIVE] BUG-245: `clsx` imported by many components separately

> **Resolution:** Not a bug — the bug description itself labels the impact as "Micro-overhead" and admits imports are deduped. `tailwind-merge` solves a different problem (class-conflict resolution) and would add a dependency without measurable gain.
**Severity:** Low
**Category:** Performance

**Location:** project-wide (many files import clsx)
**Description:** Each import is fine (it's deduped), but the sheer volume of `clsx(...)` calls at render time adds up — consider `tailwind-merge` for once-per-render instead of conditional call sites.
**Impact:** Micro-overhead.

---
## [FALSE_POSITIVE] BUG-246: `layout.tsx` loads both Inter + JetBrains Mono on every route including `/`

> **Resolution:** Not a bug — the landing page actively uses JetBrains Mono: `Timestamp` and every `BarButton` label render with `font-mono`. Dropping the font from `/` would regress brand typography on the primary entry point.
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/layout.tsx`
**Description:** Landing route `/` per FR-L8 has no nav/footer and typography is minimal — still pays for Mono.
**Impact:** First-paint font weight tax on the homepage.

---
## [FALSE_POSITIVE] BUG-247: CartDrawer mounts into the DOM tree on every page via root layout

> **Resolution:** Not a bug — the drawer's JS is already in shared chunks via other routes that use the cart, so unmounting on `/` saves only a negligible reconciliation cost while fragmenting provider/drawer wiring across per-route layouts.
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/layout.tsx` (if wrapping `CartDrawer` globally)
**Description:** Even on `/` (no nav), the cart component ships client JS.
**Impact:** Unnecessary payload on landing.

---
## [FALSE_POSITIVE] BUG-248: `Image` components in product tiles don't set `sizes` attribute

> **Resolution:** Not a bug — every `<Image fill>` in `ProductTile.tsx` and `ProductDetail.tsx` already declares `sizes` (tile "60vw/240px", main gallery "100vw/50vw", thumbs "120px", related "45vw/200px"). Next.js will pick an appropriate srcset candidate.
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/ProductTile.tsx`, `src/app/shop/[id]/ProductDetail.tsx`
**Description:** Without `sizes`, Next.js Image picks conservatively large sources.
**Impact:** Oversized image downloads on mobile.

---
## [FALSE_POSITIVE] BUG-249: Hero image priority + no preconnect to Unsplash

> **Resolution:** Not a bug — `layout.tsx:33` already declares `<link rel="preconnect" href="https://images.unsplash.com" crossOrigin="anonymous" />`, so the TCP/TLS handshake is warm before the priority hero image is fetched.
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/layout.tsx` / `src/app/home/page.tsx`
**Description:** Hero marked `priority` but no `<link rel="preconnect" href="https://images.unsplash.com">` — TCP/TLS handshake happens on critical path.
**Impact:** Slower LCP on first visit.

---
## [FALSE_POSITIVE] BUG-250: `Cursor` subscribes to global `mousemove` even when window is inactive

> **Resolution:** Not a bug — all major browsers automatically throttle/pause `requestAnimationFrame` on hidden tabs, and mouse events don't fire on inactive windows. The RAF loop doesn't actually run in backgrounded tabs; adding a `visibilitychange` gate would duplicate platform behavior.
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Cursor.tsx`
**Description:** No `visibilitychange` gating; RAF loop runs while tab is backgrounded.
**Impact:** Battery drain for users with many tabs open.

---
## [FALSE_POSITIVE] BUG-251: `Timestamp` setInterval(1000) keeps ticking when tab is hidden

> **Resolution:** Not a bug — the bug itself concedes browsers throttle background intervals. The interval is already at the 1000ms throttle floor, so no throttling savings are available. Adding `visibilitychange` gating would duplicate platform behavior.
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Timestamp.tsx`
**Description:** Browsers throttle, but the interval is still scheduled.
**Impact:** Minor CPU wake-ups.

---
## [FALSE_POSITIVE] BUG-252: `useMemo` missing around cart `count`/`subtotal` derivations inside context memo

> **Resolution:** Not a bug — count/subtotal are computed inside the outer `useMemo` at `cart.tsx:159-163`, so they only re-run when `items` identity changes. Cart sizes are tiny (1–20 items), so two O(n) reductions are microseconds. Incremental delta tracking would add complexity with no measurable gain.
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/cart.tsx:110-114`
**Description:** `count` and `subtotal` already gated by `items` identity; fine — but recomputes run over all items on every add/qty change even when only one line changed.
**Impact:** O(n) reduce per mutation; scales with cart size (small in practice).

---
## [FALSE_POSITIVE] BUG-253: `wishlist` storage writes happen in an effect that depends on `ids` — entire array re-serialized per toggle

> **Resolution:** Not a bug — realistic wishlists (10-100 IDs) serialize in sub-millisecond time, and writes only fire on toggle. Delta tracking would require a custom storage protocol with reconstruction logic, adding substantial complexity for no user-visible benefit.
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/wishlist.tsx`
**Description:** Full list re-stringified per single-id change.
**Impact:** Scales linearly with wishlist size.

---
## [FALSE_POSITIVE] BUG-254: `AnimatePresence` in `PageTransition` delays next-route paint by exit duration

> **Resolution:** Not a bug — `mode="wait"` + 0.4s exit is intentional brand design (cult-feeling fade-through). Reduced-motion users already get `duration: 0`, so the accessibility bypass exists. Removing it would break the aesthetic and cause old/new pages to stack.
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/PageTransition.tsx`
**Description:** Blocking `mode` (wait) or long exit animations hold the incoming route.
**Impact:** Perceived navigation sluggishness.

---
## [FALSE_POSITIVE] BUG-255: `Image` in lookbook grid likely ships full-resolution art

> **Resolution:** Not a bug — `lookbook/page.tsx:65` already sets `sizes="(max-width: 768px) 50vw, 33vw"`, so Next.js generates an appropriate srcset and mobile viewports pick a smaller variant.
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/lookbook/page.tsx`
**Description:** Without `sizes` / explicit smaller variants, lookbook decodes heavy images even at small viewports.
**Impact:** Data + decode time on mobile.

---
## [FIXED] BUG-256: `seededRotation` uses FNV-1a + mulberry32 per render — minor but duplicated

> **Resolution:** Added a module-level `Map<string, number>` cache keyed on `${id}:${range}` in `seededRotation.ts`. First call hashes and computes; subsequent calls return an O(1) Map lookup. Determinism is preserved so SSR/CSR rotations still match.
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/seededRotation.ts` + `ProductTile`
**Description:** Computed per render of every tile. Memoize by id.
**Impact:** Tiny, but scales with tile count.

---
## [SKIPPED] BUG-257: Client components import server-side deps transitively via `@/lib/products`

> **Resolution:** Skipped — this is an architectural refactor requiring every client component to receive products as props from a server boundary. The catalog is ~6 products (trivial bytes), so the current payload cost is negligible. Should be revisited if the catalog grows substantially, paired with BUG-130.
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/products.ts`
**Description:** Importing products.json into a client bundle pulls the full JSON into the browser payload for every page that imports the module.
**Impact:** Duplicate JSON across route chunks (see BUG-130) — flagging the import-side cost.

---
## [FALSE_POSITIVE] BUG-258: `RecentlyViewedStrip` renders even when `items.length === 0`

> **Resolution:** Not a bug — `RecentlyViewedStrip.tsx:30` already returns `null` when `items.length === 0`, before any JSX. The component also doesn't import framer-motion, so the bug's "motion wrappers" claim is incorrect.
**Severity:** Low
**Category:** Performance

**Location:** `src/components/RecentlyViewedStrip.tsx`
**Description:** Component mounts React tree + motion wrappers before detecting empty state.
**Impact:** Small render cost on first-time visitors.

---
## [FALSE_POSITIVE] BUG-259: `Reviews` component re-derives aggregate stats on every render

> **Resolution:** Not a bug — reviews arrays are <20 entries per product. Avg/distribution/filter counts cost microseconds per render. `useMemo` adds comparable overhead at these sizes and the bug itself flags impact as "Minor".
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Reviews.tsx`
**Description:** Average, distribution, filter-counts all recomputed inline.
**Impact:** Minor.

---
## [SKIPPED] BUG-260: `products.json` is required synchronously at module scope in multiple entry points

> **Resolution:** Skipped — revisiting requires a CMS/API boundary. Current catalog is ~6 products (trivial bytes) and dynamic imports would conflict with the existing SSG pipeline (`generateStaticParams` for `/shop/[id]` needs synchronous catalog access). Pair with BUG-130 / BUG-257 when catalog scales.
**Severity:** Medium
**Category:** Performance

**Location:** `src/lib/products.ts`
**Description:** No dynamic import or route-level code split — the entire catalog ships in every route chunk that depends on this module.
**Impact:** Bundle size scales 1:1 with catalog growth across every page.

---
## [FIXED] BUG-261: Nav link active-state indicated only by color

> **Resolution:** Desktop and mobile nav links now set `aria-current="page"` on the active route and add `underline underline-offset-4 font-semibold` — a non-color visual indicator satisfying WCAG 1.4.1 plus a screen-reader announcement.
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/Nav.tsx`
**Description:** Current-page nav item signalled by color change alone — no underline, weight change, or `aria-current="page"`.
**Impact:** WCAG 1.4.1 (use of color), ATs don't announce current page.

---
## [FIXED] BUG-262: Cart page "Remove" button is text-only, not distinguishable for color-blind users

> **Resolution:** Added a persistent `underline` on the "Remove" button (non-color visual affordance, always visible) and an `aria-label` naming the specific item (e.g. "Remove Heavy Hoodie from cart") so screen readers and low-contrast situations both get clear signal.
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/cart/page.tsx`
**Description:** Destructive action relies solely on text label and underline.
**Impact:** Low-contrast situations hide the CTA.

---
## BUG-263: Checkout confirmation page has no "Back to shop" / "View order" affordance [FIXED]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx` (done state render)
**Description:** Success screen ends with the message; no CTA chain.
**Impact:** Dead-end UX after purchase.

> **Resolution:** Added a `<BarButton href="/orders" label="Track order" />` between the existing "Keep shopping" and "Back home" CTAs in the done-state render of `src/app/checkout/page.tsx`, completing the post-purchase CTA chain (shop / track / home).

---
## BUG-264: Checkout country picker likely missing (ship-to-anywhere implied but no field) [FIXED]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx`
**Description:** Form captures address but no country selector — shipping fee (BUG-161) can't vary because data isn't collected.
**Impact:** Inaccurate rates; incomplete address data.

> **Resolution:** Replaced the free-text Country `FormField` with a native `<select>` dropdown populated from a new `COUNTRIES` constant (21 common destinations, defaulting to Egypt) in `src/app/checkout/page.tsx`. Structured country data is now captured so downstream shipping-rate logic can key off it reliably.

---
## BUG-265: Mobile Nav uses hover transitions that don't apply on touch [FIXED]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Nav.tsx`
**Description:** Nav links use `hover:` states only; tap highlights/active press states are missing.
**Impact:** No touch feedback.

> **Resolution:** Added `transition-opacity active:opacity-60` to the three mobile-menu interactive elements (nav Links, Wishlist Link, Cart button) in `src/components/Nav.tsx` so touch users see pressed-state feedback instead of relying on hover-only transitions.

---
## BUG-266: `<details>` / `<summary>` accordions (if any) not used for legal TOC — only static headings [FIXED]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/LegalPage.tsx`
**Description:** Long legal copy with no progressive disclosure.
**Impact:** Users scroll long walls of text.

> **Resolution:** Added a new `src/components/LegalSection.tsx` server component that wraps sections in native `<details>/<summary>` with a rotating "+" glyph, and converted all four legal pages (`terms`, `privacy`, `returns`, `shipping`) to collapsible sections with the first section open by default. Pure HTML/CSS — no client JS required.

---
## BUG-267: `Stars.tsx` uses SVG without `title` / accessible name beyond rating number [FIXED]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Stars.tsx`
**Description:** Decorative stars often lack a combined `aria-label="3.8 out of 5"` on the parent wrapper.
**Impact:** AT users hear no rating context.

> **Resolution:** Moved the accessible label to the outer wrapper as `role="img"` with `aria-label="Rated X.X out of 5, N reviews"` (singular/plural handled), and marked the star SVGs plus the visible value/count text `aria-hidden` so AT users get a single coherent announcement including the review count.

---
## BUG-268: `WishlistHeart` icon button doesn't announce on/off state via `aria-pressed` [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/WishlistHeart.tsx`
**Description:** Toggle button without `aria-pressed` / `aria-label` swap.
**Impact:** ATs can't distinguish "favorite" vs "unfavorite".

> **Resolution:** Not a bug — `src/components/WishlistHeart.tsx` already sets both `aria-pressed={active}` and an `aria-label` that swaps between "Remove from wishlist" / "Add to wishlist" depending on state. No change needed.

---
## BUG-269: ProductTile hover-only "quick add" has no touch equivalent and no focus-visible reveal [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/ProductTile.tsx`
**Description:** Already noted the touch gap; pass 3 — there's also no keyboard `focus-within` reveal.
**Impact:** Keyboard users can't tab to the hidden quick-add.

> **Resolution:** Not a bug — `ProductTile.tsx:136` already carries both `group-focus-within:translate-y-0` (keyboard reveal when the quick-add button receives focus) and `[@media(hover:none)]:translate-y-0` (persistent reveal on touch devices). No change needed.

---
## BUG-270: `Cursor` blend-mode white dot invisible on light backgrounds mid-scroll [FALSE_POSITIVE]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/Cursor.tsx`
**Description:** `mix-blend-difference` over a paper-colored background makes the dot disappear.
**Impact:** Cursor feedback lost in light sections.

> **Resolution:** Not a bug — `mix-blend-difference` of a white source against the paper background (#F5F4F0) renders as ≈ rgb(10,11,15) (almost black), which is fully visible. This blend mode is specifically the technique for keeping a single cursor visible across both light and dark sections, not a bug causing invisibility.

---
## BUG-271: Shop filters use `<button>` chips but no `aria-pressed` for the active filter [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/ShopGrid.tsx` (filter pills)
**Description:** Toggle state conveyed via class only.
**Impact:** ATs don't hear which filter is active.

> **Resolution:** Not a bug — `ShopGrid.tsx:151` already sets `aria-pressed={active}` on each filter chip button, so assistive tech correctly announces which filter is active. No change needed.

---
## BUG-272: Shop "In stock only" is a bare `<label>` with checkbox — no accessible description [FALSE_POSITIVE]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/ShopGrid.tsx`
**Description:** No explanatory text or icon.
**Impact:** Minor clarity.

> **Resolution:** Not a bug — the `<label>` wraps both the checkbox and the text "In stock only", giving it a fully programmatically-associated accessible name. The purpose is unambiguous in the shop-filter context; adding an icon or supplementary description would be noise without improving clarity.

---
## BUG-273: `PageTransition` exit animations persist during prefers-reduced-motion for some variants [FALSE_POSITIVE]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/PageTransition.tsx`
**Description:** Reduced-motion branch changes `initial`/`animate` but `exit` may still run non-trivial transition.
**Impact:** Motion-sensitive users still see animation.

> **Resolution:** Not a bug — `PageTransition.tsx:17-18` already branches the `exit` prop on `reduced` (opacity-only, no y-axis translate) and sets `transition.duration: 0` when reduced-motion is active, so the exit is effectively instant for motion-sensitive users.

---
## BUG-274: Checkout inline error messages render above inputs, not below (visual disconnect) [FALSE_POSITIVE]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/checkout/page.tsx`
**Description:** If errors render above the field they describe, tab order and gaze flow are interrupted.
**Impact:** Minor UX confusion.

> **Resolution:** Not a bug — `FormField.tsx:48-52` and the country `<select>` block in `checkout/page.tsx` both render the error `<span>` after the input element in source order, so errors visually appear below the associated field. No layout change needed.

---
## BUG-275: Cart "Continue shopping" link style identical to body text [FIXED]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/app/cart/page.tsx` (empty state if present) / checkout confirmation
**Description:** No visual affordance that it's clickable.
**Impact:** Users miss the CTA.

> **Resolution:** Added `underline underline-offset-4 decoration-1` to the "Keep shopping" link in the cart sidebar (`src/app/cart/page.tsx`) so it reads as a clickable link rather than plain body text. Cart empty state and checkout confirmation already use `BarButton` components, so no change needed there.

---
## BUG-276: Legal pages display updated date but no "last updated" label [FALSE_POSITIVE]
**Severity:** Low
**Category:** UI & Components

**Location:** `src/components/LegalPage.tsx` + `/privacy|/returns|/shipping|/terms`
**Description:** Missing standard "Last updated: YYYY-MM-DD" pattern.
**Impact:** Compliance signal weaker.

> **Resolution:** Not a bug — `LegalPage.tsx:22-24` already renders the literal label "Last updated {updated}" (e.g. "Last updated April 2026") at the top of every legal page. No change needed.

---
## BUG-277: Pre-order form has no clear indication that it's a waitlist not an order [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/pre-order/page.tsx`
**Description:** Copy may be ambiguous — users could think they're paying now.
**Impact:** Customer confusion.

> **Resolution:** Not a bug — the page renders an explicit "No payment taken. We'll email you to confirm." disclaimer directly below the submit button (`pre-order/page.tsx:217-219`), uses "Reserve" rather than "Buy" on the CTA, and the success state reads "pre-order confirmed — you're on the list." The waitlist framing is unambiguous throughout.

---
## BUG-278: Contact form `topic` selection has no default, form can submit without selecting [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/contact/page.tsx`
**Description:** If no validator guards topic, submissions arrive categorized as `""`.
**Impact:** Triage becomes harder.

> **Resolution:** Not a bug — `validate()` in `contact/page.tsx` explicitly checks `if (!values.topic) errors.topic = "Pick one"` and `handleSubmit` early-returns when errors exist, so an empty topic cannot be submitted.

---
## BUG-279: `SocialRow` icons announce file names instead of platform via missing `aria-label` [FALSE_POSITIVE]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/components/SocialRow.tsx`
**Description:** Each `<a>` needs `aria-label="Instagram"` etc.
**Impact:** AT users hear "link" without platform name.

> **Resolution:** Not a bug — `SocialRow.tsx:41` already sets `aria-label={s.label}` (the platform name) on each anchor, and the inner SVG is `aria-hidden`. AT users hear the platform name correctly.

---
## BUG-280: All content-visual routes missing `<h1>` as first heading [FIXED]
**Severity:** Medium
**Category:** UI & Components

**Location:** `src/app/home/page.tsx`, `src/app/shop/page.tsx`, `src/app/lookbook/page.tsx`
**Description:** Many pages open with stylized eyebrow text as an `<h2>` or div — no `<h1>` landmark.
**Impact:** Heading outline starts mid-hierarchy; SEO and AT penalty.

> **Resolution:** Home and Lookbook already had an `<h1>`. Added `<h1 className="sr-only">Catalog</h1>` to both branches (empty-state and populated) of `src/app/shop/page.tsx` and marked the decorative "— catalog —" eyebrow `aria-hidden` so the heading outline now starts at h1 without double announcement.

---
## BUG-281: Cart `subtotal` computed via floating-point `priceEGP` — risks cents drift if prices become decimals [FIXED]
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:112`
**Description:** `reduce((s, i) => s + i.qty * i.priceEGP, 0)` — EGP prices are integers today, but if any product is priced 299.50, floating-point summation can yield 899.5000000001.
**Impact:** Display off-by-cent once fractional prices appear.

> **Resolution:** Changed subtotal in `src/lib/cart.tsx` to sum line totals in piastres (`qty * priceEGP * 100`), round to an integer, then divide by 100. Lossless for current integer EGP prices and immune to float drift if fractional piastre prices are introduced later.

---
## BUG-282: `cart.add` fires `setIsOpen(true)` on every add — forces drawer open even from product detail where UX wants toast [FIXED]
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:96`
**Description:** No caller control over "open drawer vs silent add".
**Impact:** Can't implement a quiet add-to-cart path without refactoring.
**Evidence:**
```tsx
setIsOpen(true);
```

> **Resolution:** Extended `add()` in `src/lib/cart.tsx` with an optional `options: { silent?: boolean }` parameter and gated `setIsOpen(true)` behind `!options?.silent`. Default drawer-open behavior is preserved for all existing call sites; callers wanting a toast-only flow can now pass `{ silent: true }`.

---
## BUG-283: `findProduct` is O(n) scan; repeated calls inside render loops [FIXED]
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/products.ts`
**Description:** Without a pre-built id index, each call scans the whole array.
**Impact:** Scales poorly as catalog grows.

> **Resolution:** Built a module-level `Map<string, Product>` once at load in `src/lib/products.ts` and switched `findProduct` to `productById.get(id)`. Lookups are now O(1); behavior and return types unchanged for callers.

---
## BUG-284: `ShopGrid` `matchQuery` substring match doesn't tokenize — multi-word searches miss [FIXED]
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx:54-57`
**Description:** Query "hoodie black" never matches a "Black Hoodie" because the full string isn't a substring.
**Impact:** Poor search relevance.

> **Resolution:** In `src/components/ShopGrid.tsx`, split the query on whitespace into tokens and require `tokens.every()` to be present in a combined `name + description` haystack. Multi-word queries like "hoodie black" now match "Black Hoodie" regardless of word order; single-word and empty-query behavior is unchanged.

---
## BUG-285: `ShopGrid` `description` optional chained to `""` in search but no null-safe category inclusion [FIXED]
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/ShopGrid.tsx:57`
**Description:** Query doesn't match against category label or sizes, only name+description.
**Impact:** "Tees" query misses tee products whose name lacks "tee".

> **Resolution:** Extended the search haystack in `src/components/ShopGrid.tsx` to include `p.category` (raw key), the matching FILTERS label (e.g. "Tees"), and joined `p.sizes`. Searches now match on category in both singular and plural form, and on size tokens like "XXL".

---
## BUG-286: Cart count reduces across `qty` — negative qty (if ever) would yield negative count [FIXED]
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:111`
**Description:** No clamp on the reducer.
**Impact:** Corrupted persisted state could invert display.

> **Resolution:** Added `Math.max(0, i.qty)` to the `count` reducer in `src/lib/cart.tsx` as defense-in-depth alongside the existing `isCartItem` guard (which already requires `qty > 0`). Valid carts total unchanged; any rogue negative qty contributes zero rather than inverting the badge.

---
## BUG-287 [FIXED]: `generateStaticParams` emits id set — duplicate ids in products.json would cause Next build warning
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/shop/[id]/page.tsx:7-9`
**Description:** No dedup step; catalog author errors surface late.
**Impact:** Build surprises.

> **Resolution:** Rewrote `generateStaticParams` in `src/app/shop/[id]/page.tsx` to dedup ids via a `Set<string>`, emitting a `console.warn` for each duplicate encountered. Catalog author mistakes now surface loudly in the build log but no longer produce Next's duplicate-static-params warning.

---
## BUG-288 [FALSE_POSITIVE]: Reviews `verified` filter assumes boolean — undefined treated as "not verified"
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/Reviews.tsx`
**Description:** Missing field shouldn't be coerced to false in all cases (depends on moderation policy).
**Impact:** Semantics ambiguous.

> **Resolution:** No filter on `verified` exists — the field is only used at `Reviews.tsx:108` to conditionally render a "verified" badge. Treating undefined as "no badge" is the correct default: a trust-signal like verification must be opt-in, never inferred from absence of the field.

---
## BUG-289 [FIXED]: Stock urgency indicator (if implemented) reads from a hardcoded threshold
**Severity:** Low
**Category:** Business Logic

**Location:** likely `src/components/ProductTile.tsx` or `ProductDetail.tsx`
**Description:** If "only X left" string uses a magic number, can't tune per product.
**Impact:** Inaccurate scarcity cues.

> **Resolution:** Added optional `lowStockThreshold?: number` to the `Product` type and a shared `isLowStock(product)` helper in `src/lib/products.ts` (default threshold 5). Both `ProductTile` and `ProductDetail` now call the helper instead of inlining `stock <= 5`, so catalog authors can tune scarcity per product.

---
## BUG-290 [FALSE_POSITIVE]: Pre-order email pre-fills from checkout address but not from the auth/session
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/pre-order/page.tsx`
**Description:** No single-source-of-truth for user identity; duplicate entries.
**Impact:** Fragmented customer records.

> **Resolution:** Out of scope — this project is a static marketing site with no auth, no session, and no backend (see `CLAUDE.md`). There is no session identity to source an email from. Revisit if/when auth is introduced.

---
## BUG-291 [FALSE_POSITIVE]: `useReducedMotion()` evaluated in render for `BarButton` — toggling system preference mid-session requires remount
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/BarButton.tsx`
**Description:** Framer's hook subscribes but variants selected at render time may cache.
**Impact:** Preference change not immediate.

> **Resolution:** Framer Motion's `useReducedMotion()` is a live `matchMedia` subscription — when the OS preference flips, the hook triggers a re-render and `fillTransition`/`labelTransition` are recomputed fresh on every render. No caching across renders, no remount required.

---
## BUG-292 [SKIPPED]: Mock order `mockLookup` keys entirely off seed — email field not verified
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/app/orders/page.tsx`
**Description:** User can look up any order number with any email.
**Impact:** Privacy: a real backend must require the email to match.

> **Resolution:** Skipped — this is an explicit template stub with a `TODO(template-buyer): replace with real lookup against your orders store` comment on line 34. There is no orders backend to verify email against; real email-to-order matching must be implemented when the template buyer wires up their real store. The TODO already flags the privacy contract.

---
## BUG-293 [FALSE_POSITIVE]: Checkout `clear()` runs unconditionally after "success" — errors leave cart empty
**Severity:** High
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:107-128`
**Description:** `clear()` is called inside the success path; if a real backend fails post-clear, cart is already emptied.
**Impact:** Users lose cart on payment errors.

> **Resolution:** Not a bug — `clear()` lives inside the try block *after* the awaited mock submission resolves (`checkout/page.tsx:154`). A thrown error short-circuits to the catch block and `clear()` is never called. The existing TODO above (lines 148-150) already directs the template buyer to clear only on the payment webhook's success path when wiring a real backend.

---
## BUG-294 [SKIPPED]: Shipping method label "express" vs "standard" not localized
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:316-343`
**Description:** English-only labels despite EGP currency and Egypt audience.
**Impact:** Accessibility for Arabic speakers.

> **Resolution:** Skipped — the entire site (nav, forms, copy, product names, legal pages) is English-only with no i18n layer. Localizing only these two labels would be inconsistent; proper Arabic support requires project-wide i18n infrastructure (locale routing, RTL styles, translation catalogs) which is out of scope for this static marketing site.

---
## BUG-295 [FIXED]: Order number format `UG-YY-NNNN` rolls over every 100 years silently
**Severity:** Low
**Category:** Business Logic

**Location:** `src/app/checkout/page.tsx:29-32`
**Description:** Century ambiguity.
**Impact:** Long-term collisions (theoretical).

> **Resolution:** Changed `generateOrderNumber` in `src/app/checkout/page.tsx` to emit the full 4-digit year (e.g. `UG-2026-...`), widened the orders lookup regex at `src/app/orders/page.tsx:31` to `^UG-\d{4}-...`, and updated the order-number placeholder to `UG-2026-XXXXXX-XXX`. Century ambiguity removed.

---
## BUG-296 [FIXED]: `recentlyViewed` event fires even when viewing sold-out products
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/recentlyViewed.tsx`
**Description:** No filtering by availability — strip can be dominated by sold-out items.
**Impact:** Strip loses merchandising value.

> **Resolution:** Filtered sold-out products out of the `RecentlyViewedStrip` render pipeline in `src/components/RecentlyViewedStrip.tsx`. History is still recorded honestly (so returning stock would surface again) but the merchandising strip skips anything with `soldOut: true`.

---
## BUG-297 [FIXED]: Wishlist size bloat not bounded — no MAX cap
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/wishlist.tsx`
**Description:** No cap like recentlyViewed has; localStorage can grow unbounded for power users.
**Impact:** Quota risk.

> **Resolution:** Added `MAX_WISHLIST = 100` to `src/lib/wishlist.tsx` and applied `.slice(-MAX_WISHLIST)` after every append path (hydration merge, `add`, `toggle` adding). Oldest entries evict first when the cap is exceeded; localStorage footprint is now bounded.

---
## BUG-298 [FIXED]: Cart row `image` is snapshot — outdated if product image is replaced later
**Severity:** Low
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:90`
**Description:** Image stored on add; newer thumbs don't propagate.
**Impact:** Cart shows old image forever.

> **Resolution:** Both cart renderers (`src/app/cart/page.tsx` and `src/components/CartDrawer.tsx`) now resolve the image as `findProduct(it.productId)?.image ?? it.image` — live catalog image first, falling back to the stored snapshot only if the product has been removed. The snapshot still lives in localStorage as a safety net.

---
## BUG-299 [FIXED]: Cart row `name` / `priceEGP` snapshotted — price changes never reflected
**Severity:** Medium
**Category:** Business Logic

**Location:** `src/lib/cart.tsx:88-89`
**Description:** Price change between add and checkout goes undetected.
**Impact:** User charged an old price; revenue/risk issue.

> **Resolution:** Subtotal computation in `src/lib/cart.tsx` now uses `findProduct(i.productId)?.priceEGP ?? i.priceEGP` so it reflects catalog changes; both cart renderers (`cart/page.tsx`, `CartDrawer.tsx`) resolve name and per-line price through `findProduct` with the snapshot as fallback. Snapshots remain in localStorage as a safety net if the product is later removed from the catalog.

---
## BUG-300 [FALSE_POSITIVE]: Reviews distribution (if shown) computes percentages without guarding divide-by-zero
**Severity:** Low
**Category:** Business Logic

**Location:** `src/components/Reviews.tsx`
**Description:** Dividing by `reviews.length` with zero reviews yields `NaN%`.
**Impact:** Visible `NaN%` bar.

> **Resolution:** Not a bug — `Reviews.tsx:28-42` short-circuits with an empty-state return when `reviews.length === 0`, so the distribution math never runs with zero reviews. The pct computation at line 60 also has an inline `reviews.length === 0 ? 0 : ...` guard as belt-and-suspenders. No `NaN%` can render.

---
## BUG-301 [FIXED]: `next.config` likely missing `images.formats` (AVIF/WebP) tuning
**Severity:** Medium
**Category:** API & Data

**Location:** `next.config.*`
**Description:** No explicit `images.formats: ['image/avif','image/webp']`.
**Impact:** Larger image payloads than necessary.

> **Resolution:** Added `images.formats: ["image/avif", "image/webp"]` to `next.config.ts`. Next's image optimizer now prefers AVIF (smallest), falls back to WebP, then the original format based on the browser's `Accept` header — all transparent to `next/image` call sites.

---
## BUG-302 [FIXED]: No cache headers configuration in `next.config` for static assets
**Severity:** Medium
**Category:** API & Data

**Location:** `next.config.*`
**Description:** No `headers()` export for long-cache on `/logo.svg`, `/fallback-garment.svg`, fonts.
**Impact:** Repeat visits don't benefit from strong caching.

> **Resolution:** Added an async `headers()` export to `next.config.ts` that applies `Cache-Control: public, max-age=86400, stale-while-revalidate=604800` to public-folder assets matching `.svg|.ico|.png|.jpg|.jpeg|.webp|.avif|.woff|.woff2`. Moderate max-age avoids stuck caches if an asset is replaced without hash; `/_next/static/*` keeps its default immutable cache from Next.

---
## BUG-303 [SKIPPED]: `products.json` imported as ES module — can't be revalidated via webhook
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/products.ts`
**Description:** Tied to bundler. To update product data, must redeploy — no CMS or fetch-based pattern.
**Impact:** Content workflow friction.

> **Resolution:** Skipped — this is the project's intentional architecture per `CLAUDE.md` ("Content is config-driven"). There is no CMS, API, or backend; catalog edits happen via git commit + redeploy, which is the right workflow for a small brand's static marketing site. Swapping to webhook-based revalidation is a template-buyer decision that requires integrating a CMS/DB.

---
## BUG-304 [FIXED]: No TypeScript types enforced for `products.json` entries at parse time
**Severity:** Low
**Category:** API & Data

**Location:** `src/content/products.json`, `src/lib/products.ts`
**Description:** TS infers shape from JSON but a typo in a keyword like `"categoy"` silently creates a new field with no compile error.
**Impact:** Data typos slip through.

> **Resolution:** `satisfies Product[]` on a JSON import can't enforce `ProductCategory` because TS widens JSON string literals to `string`. Added a dev-only (`NODE_ENV !== "production"`) runtime validator in `src/lib/products.ts` that walks the catalog at module load and `console.warn`s on missing/blank `id`/`name`/`image`, non-finite `priceEGP`, or out-of-enum `category`. Typos now surface immediately in `npm run dev` and are tree-shaken from production builds.

---
## BUG-305 [SKIPPED]: No zod / runtime validation of product data
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/products.ts`
**Description:** If products.json is ever loaded from a remote source, no runtime schema validation exists.
**Impact:** Future CMS integration lands without guardrails.

> **Resolution:** Partially covered by BUG-304's dev-time validator in `src/lib/products.ts`, which walks the local catalog at module load and warns on invalid fields/categories. Introducing a full runtime schema library (zod/valibot) is only meaningful when the catalog is sourced from an untrusted remote — a template-buyer decision that pairs with CMS integration. Skipped until then.

---
## BUG-306 [FALSE_POSITIVE]: `site.config.ts` socials contain URLs but no `rel` / security metadata
**Severity:** Low
**Category:** API & Data

**Location:** `src/config/site.config.ts`
**Description:** Consumers must remember to add `rel="noopener noreferrer"` themselves.
**Impact:** Easy to miss (see BUG-160).

> **Resolution:** Not a bug today — all three consumers of `siteConfig.socials` (`SocialRow.tsx:40`, `Footer.tsx:64`, `contact/page.tsx:190`) already apply `target="_blank"` and `rel="noopener noreferrer"`. Baking `rel` into the config would add indirection without addressing any actual risk. If a future consumer is added, verify it follows the same pattern.

---
## BUG-307 [FALSE_POSITIVE]: No GDPR / cookie-consent prompt — but `localStorage` persistence happens immediately
**Severity:** Medium
**Category:** API & Data

**Location:** `src/lib/*.tsx`
**Description:** Storage is treated as first-party unconditional state.
**Impact:** Potential compliance exposure in EU jurisdictions.

> **Resolution:** All `localStorage` usage is strictly-necessary functional state the user initiates: cart items, wishlist toggles, currency preference, recently-viewed history, saved checkout address. ePrivacy Directive Art 5(3) exempts strictly-necessary storage from consent. No trackers, analytics, or marketing pixels are present. A consent prompt becomes required only if/when BUG-308 adds analytics.

---
## BUG-308 [SKIPPED]: No analytics SDK — marketing-site with no conversion tracking
**Severity:** Low
**Category:** API & Data

**Location:** project-wide
**Description:** No GA4 / Plausible / Fathom integration in layout.
**Impact:** No attribution / funnel visibility.

> **Resolution:** Skipped — template-buyer integration decision. Pre-baking an analytics SDK (GA4 needs consent banner + key; Plausible/Fathom require paid accounts) would force every downstream user to rip it out or supply credentials. Leaving the layout analytics-free is the correct default for a starter template.

---
## BUG-309 [FIXED]: `generateMetadata` on product detail omits `openGraph`/`twitter` image
**Severity:** Medium
**Category:** API & Data

**Location:** `src/app/shop/[id]/page.tsx:19-22`
**Description:** Only title/description; `product.image` isn't surfaced as `openGraph.images`.
**Impact:** Weak social share previews for product links.

> **Resolution:** Extended `generateMetadata` in `src/app/shop/[id]/page.tsx` to emit both `openGraph` (with the product image and alt text) and `twitter` (with `summary_large_image` card type) blocks. Title/description are reused across both. Share previews on Facebook, Twitter, LinkedIn, Slack, etc. now include the product shot.

---
## BUG-310 [FIXED]: Checkout uses `localStorage.setItem("address", ...)` — no expiration / versioning
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/checkout/page.tsx:117-123`
**Description:** Saved address persists indefinitely with no TTL.
**Impact:** Stale address autofilled after user moves.

> **Resolution:** Saved address is now stored as `{ savedAt, data }` and read via a `readSavedAddress()` helper in `src/app/checkout/page.tsx` that rejects and purges records older than 180 days. Legacy flat records (pre-TTL) still load so existing users don't lose their address on the first post-deploy visit; the next save migrates them to the wrapped shape.

---
## BUG-311 [SKIPPED]: Orders mock tracking IDs are deterministic — URL sharing leaks order hash
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/orders/page.tsx:45`
**Description:** If tracking ID scheme becomes real, a predictable seed means tracking IDs can be guessed from the order number.
**Impact:** Privacy: IDOR-style lookup.

> **Resolution:** Skipped — this is the template mock covered by the existing `TODO(template-buyer): replace with real lookup against your orders store` on `orders/page.tsx:34`. A real backend must issue opaque, carrier-provided tracking IDs that aren't derivable from the order number. Same mitigation path as BUG-292.

---
## [SKIPPED] BUG-312: No rate-limit pattern on lookup/forms (fine for static, but forms will POST later)

> **Resolution:** Skipped — rate limiting is a server-side concern that belongs on the backend (API gateway, edge middleware, or WAF) once forms actually POST. The static site has no endpoint to rate-limit. Existing `lookupInFlight`/`submitInFlight` guards already prevent double-submit UX issues client-side.
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/orders/page.tsx`, `src/app/contact/page.tsx`
**Description:** No client-side throttling — a real backend will need rate limit, but forms don't queue or coalesce.
**Impact:** Future abuse vector.

---
## [SKIPPED] BUG-313: No fetch wrapper / shared client — each page reinvents async patterns

> **Resolution:** Skipped — the static site has zero `fetch` callers today. Building a shared HTTP client before the first real endpoint exists would be premature abstraction. Revisit when the first backend integration lands so the wrapper is shaped by real auth/retry/error-envelope requirements.
**Severity:** Low
**Category:** API & Data

**Location:** project-wide
**Description:** Forms will end up calling `fetch` directly; no shared retry / timeout / auth layer.
**Impact:** Consistency / maintainability debt.

---
## [SKIPPED] BUG-314: Static assets referenced by `/public` paths — no versioning / cache-busting

> **Resolution:** Skipped — only two `/public`-served URLs exist (`logo.svg` is inlined via `fs.readFileSync`, so not a URL at all; `fallback-garment.svg` is the only runtime-fetched public asset). The `Cache-Control: max-age=86400` header set in `next.config.ts` already bounds staleness to 24h, which is acceptable for rarely-changing brand assets. Build-time fingerprinting for two SVGs is over-engineering.
**Severity:** Low
**Category:** API & Data

**Location:** `public/logo.svg`, `public/fallback-garment.svg`
**Description:** Changing the SVG requires URL changes or aggressive cache invalidation.
**Impact:** Stuck-on-CDN assets after edits.

---
## [FIXED] BUG-315: `Timestamp` reads `new Date()` each tick — no timezone normalization

> **Resolution:** `Timestamp.tsx` now formats via `Intl.DateTimeFormat` with `timeZone: "Africa/Cairo"` (cached formatter at module scope) and appends a `CAI` suffix so every visitor sees the brand city's clock regardless of their own timezone.
**Severity:** Low
**Category:** API & Data

**Location:** `src/components/Timestamp.tsx`
**Description:** Client timezone only; no UTC anchor or brand-city display.
**Impact:** Time displayed may not match brand intent (the site's Cairo framing).

---
## [SKIPPED] BUG-316: No API route / serverless function scaffolded for future handlers

> **Resolution:** Skipped — the site is intentionally static per CLAUDE.md. Scaffolding empty `route.ts` handlers before a real consumer exists is premature abstraction; the route shape should be informed by actual endpoint requirements when the first backend integration lands.
**Severity:** Low
**Category:** API & Data

**Location:** `src/app/api/`
**Description:** No `route.ts` files — forms must be wired from scratch.
**Impact:** Implementation gap to ship real backend.

---
## [SKIPPED] BUG-317: `products.json` inline pricing includes no currency field

> **Resolution:** Skipped — `priceEGP` is an explicit canonical unit, and `src/lib/currency.tsx` already converts from EGP to displayed currencies via FX rates. Per-region override price lists are a merchandising/business decision, not a technical gap; the schema change should be driven by that requirement if it arises.
**Severity:** Low
**Category:** API & Data

**Location:** `src/content/products.json`
**Description:** `priceEGP` is the only field — implicit single-currency pricing; no multi-currency price lists.
**Impact:** Can't ship region-specific prices without refactor.

---
## [FALSE_POSITIVE] BUG-318: `lookbook.json` images lack `width`/`height` metadata (CLS risk)

> **Resolution:** Not a bug — `lookbook/page.tsx` uses `<Image fill>` inside containers with fixed CSS aspect ratios (`aspect-[3/4]`, `aspect-[4/3]`, `aspect-square`) driven by the JSON's `aspect` field. The aspect-ratio parent reserves layout space before load, which is Next.js's canonical CLS-prevention pattern for `fill` images. Numeric dimensions are unnecessary.
**Severity:** Medium
**Category:** API & Data

**Location:** `src/content/lookbook.json`
**Description:** `<Image>` may need intrinsic dims; without them, layout shifts on load.
**Impact:** CLS penalty on lookbook route.

---
## [FALSE_POSITIVE] BUG-319: `Image` components importing remote Unsplash URLs also fetch them during `next build` for optimization — can hammer Unsplash rate limits

> **Resolution:** Not a bug — Next.js Image Optimization is request-time, not build-time. During `next build`, pages are prerendered with `/_next/image?url=...` query URLs only; the actual fetch + transform happens lazily on first user request and is then cached. CI builds do not hammer Unsplash.
**Severity:** Medium
**Category:** API & Data

**Location:** `src/content/products.json`, `src/app/home/page.tsx`
**Description:** CI builds re-fetch every image.
**Impact:** 429s from Unsplash; flaky builds.

---
## [FIXED] BUG-320: `recentlyViewed` save writes `slice(0, MAX)` but reads unbounded — corrupted pre-cap data survives

> **Resolution:** `load()` in `src/lib/recentlyViewed.tsx` now applies `.slice(0, MAX)` after the string-filter, matching `save()` symmetrically. Legacy/tampered oversized arrays get trimmed on first read.
**Severity:** Low
**Category:** API & Data

**Location:** `src/lib/recentlyViewed.tsx:10-16` (load vs save)
**Description:** Load has no cap; only save trims.
**Impact:** Legacy / tampered storage bypasses the limit on first read.

---
## [FIXED] BUG-321: CartProvider uses `hydrated` flag but initial `items=[]` renders cart count 0 on server

> **Resolution:** Exposed `hydrated` on `CartContextValue`. `Nav.tsx` now renders a non-breaking space in the cart badge (both desktop and mobile) until `hydrated` is true, then snaps to the real count. Eliminates the 0→N flicker for returning users with saved cart items.
**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/cart.tsx:53-60`
**Description:** Server renders count=0; after hydration load sets real count — Nav badge flickers 0 → N.
**Impact:** Visual count flicker / hydration mismatch potential.

---
## [FIXED] BUG-322: Wishlist provider has identical SSR→client count flicker

> **Resolution:** Applied the same pattern as #321 — exposed `hydrated` on `WishlistContextValue`; `Nav.tsx` Saved/Wishlist badges (desktop + mobile) render `\u00A0` until hydration completes, then snap to the real count.
**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/wishlist.tsx`
**Description:** Same pattern.
**Impact:** Heart count badge flickers.

---
## [SKIPPED] BUG-323: Currency provider SSR→client mismatch — renders in EGP then jumps to USD if user preference is USD

> **Resolution:** Skipped — the `hydrated ? currency : "EGP"` guard in `currency.tsx:66` already prevents a true React hydration mismatch; the remaining one-frame flash is intrinsic to combining SSG with a client-persisted preference. Eliminating it requires cookies + dynamic rendering (breaks the static-first architecture) or hiding all site-wide prices until hydration (worse UX than the flash). Accepted tradeoff.
**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/currency.tsx`
**Description:** Server defaults to EGP; client hydration reads prior preference. Prices flash.
**Impact:** User sees EGP flash of all prices before USD/EUR.

---
## [FALSE_POSITIVE] BUG-324: Cart `add` side-effect (opening drawer) inside setItems updater

> **Resolution:** Not a bug — `setIsOpen(true)` at `cart.tsx:145` is already a sibling statement to `setItems`, not nested inside its updater. React 18 batches the two state updates automatically. The bug description itself conceded "Fine today".
**Severity:** Low
**Category:** State Management

**Location:** `src/lib/cart.tsx:96`
**Description:** `setIsOpen(true)` called outside but right after `setItems`. Fine today but couples the two.
**Impact:** Refactoring hazard.

---
## [FALSE_POSITIVE] BUG-325: `useCart` called in render subtrees that may unmount during open — `closeCart` may target unmounted DOM

> **Resolution:** Not a bug — `closeCart` only calls `setIsOpen(false)` on `CartProvider`, which is mounted at the root `layout.tsx` and never unmounts on route changes. No DOM targeting is involved; it's pure React context state.
**Severity:** Low
**Category:** State Management

**Location:** `src/lib/cart.tsx`, `src/components/CartDrawer.tsx`
**Description:** If CartDrawer unmounts between setState calls (e.g., on route change), deferred `closeCart` still fires on stale provider.
**Impact:** Rare edge case.

---
## [FALSE_POSITIVE] BUG-326: `useEffect` that sets `hydrated=true` fires after first paint — CLS window where nav renders pre-hydration

> **Resolution:** Not a bug — cart/wishlist badges already use `min-w-[1.25rem]` (desktop) and `min-w-[1.5rem]` (mobile) to reserve stable width. Combined with #321's `\u00A0` placeholder pre-hydration, the badge content stays inside the reserved min-width before and after hydration — no layout shift.
**Severity:** Low
**Category:** State Management

**Location:** `src/lib/cart.tsx:57-60`
**Description:** Initial paint shows skeletonless cart icon; hydration populates a count that shifts badge width.
**Impact:** Minor CLS.

---
## [SKIPPED] BUG-327: No memoization wrapper (`React.memo`) on `ProductTile`

> **Resolution:** Skipped — `ProductTile` subscribes to `useCart` and `useCurrency`; context consumers re-render on every context change regardless of `React.memo`. Wrapping it would add complexity without preventing the actual re-render triggers (cart mutations, currency switches). Premature optimization without a measured perf problem.
**Severity:** Medium
**Category:** State Management

**Location:** `src/components/ProductTile.tsx`
**Description:** Every ShopGrid re-render re-renders every tile, even when props are unchanged.
**Impact:** Unnecessary work on filter/sort toggles.

---
## [FALSE_POSITIVE] BUG-328: `Reviews` component receives full product or array of reviews — not stable reference across renders

> **Resolution:** Not a bug — `reviews` is passed directly from `product.reviews` on the static `products.json` import, which yields stable references across renders. `Reviews` itself isn't memoized, so there's no memoization to invalidate. Internal `dist`/`filter` work is O(small-n) and trivial.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/Reviews.tsx`
**Description:** Parent-created arrays reset memoization each render.
**Impact:** Lost memoization.

---
## [FALSE_POSITIVE] BUG-329: Cursor `setEnabled` state based on matchMedia — second mount during route change re-runs the matchMedia check

> **Resolution:** Not a bug — `<Cursor />` is mounted in `app/layout.tsx:41` (the root layout), which Next.js App Router preserves across client-side navigations. It never remounts on route change, so the matchMedia check never re-runs.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/Cursor.tsx`
**Description:** Routing may remount Cursor if it's not placed in stable layout scope.
**Impact:** Brief disable/enable flicker.

---
## [FALSE_POSITIVE] BUG-330: `PageTransition` `key={pathname}` forces full subtree unmount — loses focus position, scroll, form state

> **Resolution:** Not a bug — `key={pathname}` is required for `AnimatePresence mode="wait"` to run exit→enter transitions between pages. The alleged impacts (form reset, scroll-to-top) are inherent to Next.js App Router route changes, not caused by this wrapper. Same-path navigations (query/hash) keep the same key and don't remount.
**Severity:** Medium
**Category:** State Management

**Location:** `src/components/PageTransition.tsx`
**Description:** Aggressive re-keying resets client state on every navigation — incompatible with back-forward cache intent.
**Impact:** Forms wiped by nav; scroll-to-top every route.

---
## [FALSE_POSITIVE] BUG-331: `FormField` is uncontrolled by default for some callers — mixing controlled and uncontrolled semantics

> **Resolution:** Not a bug — audited every call site (contact, checkout, orders, pre-order); all pass `value`+`onChange` (fully controlled). The component only spreads props and never injects a `defaultValue`, so there's no controlled/uncontrolled mixing possible given current usage.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/FormField.tsx` + consumers
**Description:** If some pages pass `value` and others don't, React warns about controlled/uncontrolled switch.
**Impact:** Console warnings; subtle data corruption.

---
## [FIXED] BUG-332: `Newsletter` `state` machine has no "idle after ok" transition — success stuck until manual reset

> **Resolution:** Added a "Subscribe another" reset button underneath the success message in `Newsletter.tsx`. Clicking it flips state back to `idle`, re-showing the form with an already-cleared email field so the user can subscribe a different address without reloading.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/Newsletter.tsx`
**Description:** After "ok" the UI never resets to "idle" — user can't try again.
**Impact:** Single-use form until reload.

---
## [FALSE_POSITIVE] BUG-333: `orders` page success and error both reset `busy` but not sequence guard — a late-resolving promise sets `busy=false` during a new lookup

> **Resolution:** Not a bug — `orders/page.tsx` already guards with `lookupInFlight.current` (early-return on concurrent submit) plus a monotonic `lookupSeq` ref. The `finally` block at line 83 only runs `setBusy(false)` when `seq === lookupSeq.current`, so stale resolves can't clobber the current request's busy state.
**Severity:** Medium
**Category:** State Management

**Location:** `src/app/orders/page.tsx`
**Description:** Out-of-order resolves clobber the in-flight second request's `busy=true`.
**Impact:** UI shows ready while request still pending.

---
## [FIXED] BUG-334: Checkout form `errors` state isn't cleared when user fixes a field — only on next submit

> **Resolution:** `update()` in `checkout/page.tsx` now clears the matching `errors[key]` entry when the user edits a field, so stale red text disappears as soon as they start correcting it. Other fields' errors remain untouched.
**Severity:** Low
**Category:** State Management

**Location:** `src/app/checkout/page.tsx` (validate flow)
**Description:** Stale error persists after correction until re-submit.
**Impact:** Misleading red text after input is now valid.

---
## [FIXED] BUG-335: Pre-order form errors state not cleared on field edit

> **Resolution:** Applied the same pattern as #334 — `update()` in `pre-order/page.tsx` now clears the matching `errors[key]` entry on every keystroke so stale red text disappears as the user corrects a field.
**Severity:** Low
**Category:** State Management

**Location:** `src/app/pre-order/page.tsx`
**Description:** Same as 334.
**Impact:** Stale error display.

---
## [FIXED] BUG-336: Contact form errors state not cleared on field edit

> **Resolution:** Same pattern as #334/#335 — `update()` in `contact/page.tsx` now drops the matching `errors[key]` entry on keystroke so stale red text clears as the user corrects each field.
**Severity:** Low
**Category:** State Management

**Location:** `src/app/contact/page.tsx`

---
## [FALSE_POSITIVE] BUG-337: Wishlist `toggle` function identity is stable via useCallback but depends on `ids` closure

> **Resolution:** Not a bug — all three callbacks (`add`, `remove`, `toggle`) in `wishlist.tsx:69-79` use the functional updater form `setIds((prev) => ...)`. The `prev` argument always reflects the latest state, so empty `useCallback` deps are correct and no stale `ids` closure exists.
**Severity:** Medium
**Category:** State Management

**Location:** `src/lib/wishlist.tsx` toggle/add/remove
**Description:** If `toggle` uses functional updater it's safe, but if it reads `ids` directly it captures stale data.
**Impact:** Toggling twice quickly may only register the first change.

---
## [FALSE_POSITIVE] BUG-338: `ShopGrid` filter/sort/hideSoldOut state not reset when `regular` prop identity changes

> **Resolution:** Not a bug — `ShopGrid.tsx:55-58` already resets `filter` to `"all"` whenever the current category no longer exists in `regular`; lines 62-64 auto-clear `hideSoldOut` when no sold-out items remain. `sort` values are universally valid and `query` is user input that shouldn't self-clear on data changes.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/ShopGrid.tsx`
**Description:** Different catalog → filter may point at non-existent category.
**Impact:** See BUG-206.

---
## [FALSE_POSITIVE] BUG-339: `Nav` uses `useRouter`/`usePathname` — every route change re-renders Nav entirely

> **Resolution:** Not a bug — `Nav` uses `usePathname` specifically to highlight the active route link; re-rendering on route change is the intended behavior. The component does no expensive work, and memoizing children wouldn't help since the pathname dependency drives the active-state logic.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/Nav.tsx`
**Description:** No memoization of nav children around pathname change.
**Impact:** Wasted tree work.

---
## [FALSE_POSITIVE] BUG-340: `CartDrawer` renders full cart content even when `isOpen=false`

> **Resolution:** Not a bug — `CartDrawer.tsx:101-102` wraps the drawer body in `<AnimatePresence>{isOpen && (...)}`. When `isOpen=false` the subtree is unmounted (apart from the brief exit-animation window), so line items don't re-render during cart churn while closed.
**Severity:** Low
**Category:** State Management

**Location:** `src/components/CartDrawer.tsx`
**Description:** AnimatePresence keeps items mounted for exit; list of line items re-renders on state churn while hidden.
**Impact:** Wasted renders.

---
## [FALSE_POSITIVE] BUG-341: No per-route `error.tsx` for `/shop`, `/checkout`, `/cart`, `/wishlist`, `/orders`, `/contact`, `/pre-order`, `/lookbook`
**Severity:** High
**Category:** Error Handling

> **Resolution:** `src/app/error.tsx` at the root of the App Router provides an error boundary that propagates to all nested routes. Next.js bubbles uncaught errors up the segment tree until a boundary handles them, so every listed route is already covered. Per-route boundaries would only add value for section-specific error UX, which is not a PRD requirement.

**Location:** `src/app/*/error.tsx` absent
**Description:** Next App Router supports segment-level error boundaries. None exist.
**Impact:** Any child error in any of these routes crashes to a blank page.

---
## [FIXED] BUG-342: No `global-error.tsx` for root boundary
**Severity:** High
**Category:** Error Handling

> **Resolution:** Created `src/app/global-error.tsx` with its own `<html>/<body>` (required since it replaces the crashing root layout). Uses inline styles in brand palette — no Tailwind/font dependency, since layout CSS isn't guaranteed when the layout itself errors. Renders a "try again" reset button.

**Location:** `src/app/global-error.tsx` absent
**Description:** Catches errors from root layout itself. Missing.
**Impact:** Top-level crashes produce unstyled browser default.

---
## [FALSE_POSITIVE] BUG-343: No `loading.tsx` segment for `/shop/[id]` dynamic route
**Severity:** Low
**Category:** Error Handling

> **Resolution:** `/shop/[id]` is fully SSG — all product pages are prerendered via `generateStaticParams` (confirmed by `next build` output showing `●` SSG marker). There is no request-time data prep, so no loading state exists to cover.

**Location:** `src/app/shop/[id]/loading.tsx` absent
**Description:** First-paint delay during data prep has no skeleton.
**Impact:** Perceived slowness.

---
## [FALSE_POSITIVE] BUG-344: `lib/products.ts` `findProduct` swallowed by `undefined` return — no explicit "why"
**Severity:** Low
**Category:** Error Handling

> **Resolution:** Catalog is a static JSON bundled at build time and loaded into a `Map<string, Product>`. Dev-mode schema validation (lines 59-69) already logs corrupt entries at load. At runtime, `Map.get` has exactly one failure mode — the id isn't in the catalog. No "bad id vs corrupt data" ambiguity exists to distinguish.

**Location:** `src/lib/products.ts`
**Description:** Returning undefined is ambiguous (bad id vs corrupt data).
**Impact:** Can't log the distinction.

---
## [FALSE_POSITIVE] BUG-345: `dangerouslySetInnerHTML` on Logo means a malformed SVG breaks the whole layout without fallback
**Severity:** Medium
**Category:** Error Handling

> **Resolution:** The logo SVG is a version-controlled brand asset, not user input. CLAUDE.md explicitly scopes validation to system boundaries — don't validate internal code. Existing fallback already handles `fs.readFileSync` failures (lines 10-20). A malformed SVG would be immediately visible on first dev render and caught in review.

**Location:** `src/components/Logo.tsx`
**Description:** If SVG contains a parser error, React doesn't sanitize or warn.
**Impact:** Layout can render with broken brand mark.

---
## [FALSE_POSITIVE] BUG-346: `Cursor` `useEffect` has no `try/catch` around `matchMedia`
**Severity:** Low
**Category:** Error Handling

> **Resolution:** `window.matchMedia` is a stable browser API present in every Next.js-supported runtime and does not throw for well-formed media queries. Wrapping it in try/catch would be defensive programming for an impossible failure mode.

**Location:** `src/components/Cursor.tsx`

---
## [FALSE_POSITIVE] BUG-347: `Reviews` and `Stars` do not guard against malformed `review.rating` NaN
**Severity:** Low
**Category:** Error Handling

> **Resolution:** Reviews are sourced from `src/content/products.json` and typed in `lib/products.ts` as `rating: 1 | 2 | 3 | 4 | 5`. TypeScript enforces this at compile time and there is no user-input path that could produce NaN. Guarding trusted internal data violates CLAUDE.md's boundary-only validation rule.

**Location:** `src/components/Stars.tsx`, `src/components/Reviews.tsx`
**Description:** NaN stars render zero fill silently.
**Impact:** Latent data corruption invisible.

---
## BUG-348: `Orders` page `mockLookup` returns null for "not found" but doesn't differentiate from "invalid format"
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/app/orders/page.tsx:35-48`
**Description:** Regex failure vs not-found collapsed into a single error string.
**Impact:** User can't tell a typo from a genuine miss.

---
## BUG-349: `Checkout` storage-write catch block logs nothing — address autofill failure is silent
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx:117-121`

---
## BUG-350: `PageTransition` `AnimatePresence` swallows render errors of children during exit
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/PageTransition.tsx`
**Description:** Errors thrown mid-exit may not propagate to error boundaries (Framer Motion consumes).
**Impact:** Hidden crashes during route change.

---
## BUG-351: `ProductDetail` does not guard against `products.find` returning undefined in related list
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/shop/[id]/ProductDetail.tsx:46`
**Description:** `.filter((p) => p.id !== product.id)` fine, but downstream `.find` usage in other places may not be guarded.
**Impact:** Undefined dereferences.

---
## BUG-352: `Currency` `convert` / `format` doesn't validate input is a finite number
**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/currency.tsx:67-73`
**Description:** `NaN * RATES[cur]` yields NaN → visible `NaN EGP`.
**Impact:** Garbage display on corrupt data.

---
## BUG-353: `cart.setQty` silently coerces NaN to 1 via `Math.floor` → NaN comparisons
**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:103-105`
**Description:** `Math.floor(NaN) === NaN`, then `Math.max(1, Math.min(99, NaN)) === NaN` — NaN is saved.
**Impact:** Corrupt qty field; silent.

---
## BUG-354: Checkout shipping method toggle has no guard if `shipMethod` has an unexpected value from localStorage
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx`
**Description:** If future persistence introduces a stored shipMethod that isn't "standard"/"express", cost math breaks.
**Impact:** Latent bug.

---
## BUG-355: `BarButton` `onClick` handler errors aren't caught
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/BarButton.tsx`
**Description:** Consumers may throw; no try/catch wrapper.
**Impact:** UI frozen in the middle of the bar fill animation.

---
## BUG-356: `Nav` link navigation relies on Link — no `onError` path if route chunks fail to load
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/Nav.tsx`
**Description:** Network failure fetching a route chunk surfaces only as generic error.
**Impact:** Silent nav dead-ends on flaky networks.

---
## BUG-357: `generateMetadata` for product returns "Not found" title on missing — inconsistent with notFound() in page
**Severity:** Low
**Category:** Error Handling

**Location:** `src/app/shop/[id]/page.tsx:18-18`
**Description:** Metadata path: `{ title: "Not found" }`. Page path: `notFound()`. Users can see a 404 titled "Not found" but it's the default theme.
**Impact:** Mismatched UX.

---
## BUG-358: No retry UI on form submit failure in any page
**Severity:** Medium
**Category:** Error Handling

**Location:** `src/app/checkout/page.tsx`, `src/app/orders/page.tsx`, `src/app/contact/page.tsx`, `src/app/pre-order/page.tsx`, `src/components/Newsletter.tsx`
**Description:** No "Retry" button after failed submit — user must manually re-enter data.
**Impact:** High-friction recovery.

---
## BUG-359: Cart row with missing `product.image` doesn't swap to fallback
**Severity:** Low
**Category:** Error Handling

**Location:** `src/lib/cart.tsx:90`, `src/components/CartDrawer.tsx`
**Description:** Stored image at add time; if empty, no fallback branch.
**Impact:** Broken image in drawer.

---
## BUG-360: `useEffect` in CartDrawer sets `document.body.style.overflow` but throws if `document` is undefined during exit (rare)
**Severity:** Low
**Category:** Error Handling

**Location:** `src/components/CartDrawer.tsx:21-22`
**Description:** Cleanup runs after unmount; if SSR teardown happens, document may be gone.
**Impact:** Edge crash.

---
## BUG-361: `AnimatePresence mode="wait"` (if set) doubles perceived navigation time
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/PageTransition.tsx`
**Description:** Wait-mode keeps the old tree mounted until exit completes before mounting new route. Blocks TTFB-to-first-paint.
**Impact:** Slow route transitions.

---
## BUG-362: `ProductTile` Image does not use `placeholder="blur"` with data URL
**Severity:** Low
**Category:** Performance

**Location:** `src/components/ProductTile.tsx`
**Description:** No LQIP placeholder — shimmer overlay only.
**Impact:** Perceived load time higher than necessary.

---
## BUG-363: `products.json` duplicated in server-side catalog and client-side tiles
**Severity:** Medium
**Category:** Performance

**Location:** `src/lib/products.ts`
**Description:** Whole JSON ends up in both server chunks and client chunks for pages that use `<ShopGrid>` — doubles payload.
**Impact:** Duplicated bytes.

---
## BUG-364: `ProductTile` uses `motion.div` for hover effect where `<div className="transition-transform hover:scale-*">` would do
**Severity:** Low
**Category:** Performance

**Location:** `src/components/ProductTile.tsx`
**Description:** Framer import unnecessary for what CSS could do.
**Impact:** Bundle cost.

---
## BUG-365: Cart context value rebuilt on every `items` change — cascades to all cart consumers
**Severity:** Medium
**Category:** Performance

**Location:** `src/lib/cart.tsx:110-114`
**Description:** Every item edit re-renders every component that uses `useCart()` — including Nav badge, CartDrawer, buttons.
**Impact:** Wide re-render blast radius on single-row edits.

---
## BUG-366: No `revalidate` / `next: { revalidate }` means everything re-rendered on each request
**Severity:** Low
**Category:** Performance

**Location:** `src/app/*/page.tsx`
**Description:** With no revalidate config, Next falls back to request-time generation on dynamic configs.
**Impact:** Slightly slower cold requests than a tuned ISR.

---
## BUG-367: Framer Motion's `AnimatePresence` ships even on pages where it does nothing
**Severity:** Low
**Category:** Performance

**Location:** various
**Description:** Root PageTransition always uses `AnimatePresence`; for routes with minimal animations, the dep is still loaded.
**Impact:** Bundle overhead.

---
## BUG-368: `Nav` re-rendered on every page navigation due to `usePathname` subscription
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Nav.tsx`
**Description:** Full nav component re-renders on every route change.
**Impact:** Minor.

---
## BUG-369: `Cursor` `requestAnimationFrame` runs continuously even when cursor hasn't moved
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Cursor.tsx`
**Description:** RAF loop spins at full rate regardless of input.
**Impact:** Battery drain.

---
## BUG-370: `CartDrawer` body-scroll-lock applied via inline style triggers style recalculation of entire document
**Severity:** Low
**Category:** Performance

**Location:** `src/components/CartDrawer.tsx`
**Description:** Reflow on every open/close.
**Impact:** Jank on slow devices.

---
## BUG-371: Checkout form re-runs `validate(form)` on every submit but not lazily per field
**Severity:** Low
**Category:** Performance

**Location:** `src/app/checkout/page.tsx`
**Description:** Full validation on each keystroke (if wired) over all fields.
**Impact:** Minor input lag.

---
## BUG-372: `Reviews` stars sub-component re-mounts on filter change instead of keyed memo
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Reviews.tsx`
**Description:** Star components re-rendered unnecessarily.
**Impact:** Wasted work.

---
## BUG-373: Landing `/` imports the CartProvider context globally
**Severity:** Medium
**Category:** Performance

**Location:** `src/app/layout.tsx`
**Description:** Even FR-L8 landing (no nav/footer) pays for cart/wishlist/currency provider setup.
**Impact:** Heavier landing page than necessary.

---
## BUG-374: `next/image` width/height inferred from intrinsic means layout uses JS
**Severity:** Low
**Category:** Performance

**Location:** project-wide
**Description:** Many `<Image fill />` variants rely on positioned parents and JS-computed aspect ratios.
**Impact:** Minor CLS.

---
## BUG-375: `Reviews` state updates cause full Reviews container re-render including per-review motion
**Severity:** Low
**Category:** Performance

**Location:** `src/components/Reviews.tsx`
**Description:** No list virtualization; long review lists mount all at once.
**Impact:** Slow scroll / memory on many reviews.

---
## BUG-376: `wishlist.tsx` persistence effect runs on every `ids` change — no throttle
**Severity:** Low
**Category:** Performance

**Location:** `src/lib/wishlist.tsx`
**Description:** Same coalescing gap already logged, but specific to persistence effect timing.
**Impact:** Micro-burst writes.

---
## BUG-377: `ShopGrid` `filtered` memo is O(n) per render but produces fresh array each time — breaks `React.memo` on children
**Severity:** Medium
**Category:** Performance

**Location:** `src/components/ShopGrid.tsx:50-76`
**Description:** Even if ProductTile were `React.memo`'d, parent memo produces new array of same-element references — children still re-render.
**Impact:** Memoization defeated at the list level.

---
## BUG-378: `currency.format` and `convert` are recreated per provider render (no useCallback)
**Severity:** Medium
**Category:** Performance

**Location:** `src/lib/currency.tsx`
**Description:** New function identities each render — every price-formatting component treats them as changed.
**Impact:** Re-render storms on every provider state change.

---
## BUG-379: Lookbook page images marked `priority` not limited to above-the-fold
**Severity:** Low
**Category:** Performance

**Location:** `src/app/lookbook/page.tsx`
**Description:** If multiple lookbook shots get `priority`, browser preloads more than it should.
**Impact:** Competes with LCP asset.

---
## BUG-380: Framer Motion chunks not `dynamic`-imported for below-fold animations
**Severity:** Low
**Category:** Performance

**Location:** various
**Description:** Heavy animation components ship in the initial bundle.
**Impact:** Larger initial JS payload.
















